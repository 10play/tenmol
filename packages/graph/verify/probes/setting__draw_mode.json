{
  "id": "setting__draw_mode",
  "feature": "draw_mode",
  "kind": "setting",
  "probeKind": "state",
  "ops": [
    {"do": "load /home/tenplay/Documents/GitHub/10ground/sessions/map2/tenmol/packages/engine/test/dat/1tii.pdb, m"},
    {"do": "set draw_mode, 1, m"}
  ],
  "checks": [
    {"call": ["get_setting_int", "draw_mode"], "label": "global default (unset) -> 0"},
    {"call": ["get_setting_int", "draw_mode", "m"], "label": "per-object set to 1 -> 1"}
  ],
  "expected": [
    0,
    1
  ]
}
