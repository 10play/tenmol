{
  "id": "setting__display_scale_factor", "feature": "display_scale_factor", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set display_scale_factor, 2, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","display_scale_factor"],"label":"global display_scale_factor default (no prior set)"},
    {"call":["get_setting_int","display_scale_factor","ala"],"label":"per-object display_scale_factor after set 2"}
  ],
  "expected": [
    1,
    2
  ]
}
