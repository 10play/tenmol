{
  "id": "setting__surface_type", "feature": "surface_type", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"set surface_type, 3"}
  ],
  "checks": [
    {"call":["get_setting_int","surface_type"],"label":"surface_type after set 3"}
  ],
  "expected": [
    3
  ]
}
