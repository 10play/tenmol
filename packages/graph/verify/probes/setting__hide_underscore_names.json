{
  "id": "setting__hide_underscore_names", "feature": "hide_underscore_names", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set hide_underscore_names, 0, ala"}
  ],
  "checks": [
    {"call":["get_setting_boolean","hide_underscore_names"],"label":"global DEFAULT hide_underscore_names (expect on/true)"},
    {"call":["get_setting_boolean","hide_underscore_names","ala"],"label":"per-object hide_underscore_names after set 0 (expect off/false)"}
  ],
  "expected": [
    true,
    false
  ]
}
