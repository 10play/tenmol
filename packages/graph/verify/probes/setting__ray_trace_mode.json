{
  "id": "setting__ray_trace_mode",
  "feature": "ray_trace_mode",
  "kind": "setting",
  "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set ray_trace_mode, 3, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","ray_trace_mode"],"label":"global ray_trace_mode default (no prior set)"},
    {"call":["get_setting_int","ray_trace_mode","ala"],"label":"per-object ray_trace_mode after set 3"}
  ],
  "expected": [
    0,
    3
  ]
}
