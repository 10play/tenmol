{
  "id": "setting__surface_negative_color", "feature": "surface_negative_color", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"set surface_negative_color, red"}
  ],
  "checks": [
    {"call":["get_setting_int","surface_negative_color"],"label":"surface_negative_color (default=red) after set red -> red color index"},
    {"call":["get_color_index","red"],"label":"red color index (oracle vs engine)"}
  ],
  "tol": 1e-3,
  "expected": [
    4,
    4
  ]
}
