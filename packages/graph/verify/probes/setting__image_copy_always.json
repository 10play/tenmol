{
  "id": "setting__image_copy_always", "feature": "image_copy_always", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set image_copy_always, 1, ala"}
  ],
  "checks": [
    {"call":["get_setting_boolean","image_copy_always"],"label":"global image_copy_always default (no prior set)"},
    {"call":["get_setting_boolean","image_copy_always","ala"],"label":"per-object image_copy_always after set 1"}
  ],
  "expected": [
    false,
    true
  ]
}
