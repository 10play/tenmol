{
  "id": "setting__gaussian_resolution", "feature": "gaussian_resolution", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"load /home/tenplay/Documents/GitHub/10ground/sessions/map2/tenmol/packages/engine/test/dat/1tii.pdb, m"},
    {"do":"set gaussian_resolution, 4.0, m"}
  ],
  "checks": [
    {"call":["get_setting_float","gaussian_resolution"],"label":"global gaussian_resolution default (no prior global set) -> 2.0"},
    {"call":["get_setting_float","gaussian_resolution","m"],"label":"per-object gaussian_resolution after set 4.0 (roundtrip)"}
  ],
  "tol": 1e-6,
  "expected": [
    2,
    4
  ]
}
