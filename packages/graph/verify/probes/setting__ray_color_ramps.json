{
  "id": "setting__ray_color_ramps", "feature": "ray_color_ramps", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set ray_color_ramps, 1, ala"}
  ],
  "checks": [
    {"call":["get_setting_boolean","ray_color_ramps"],"label":"global ray_color_ramps default (off)"},
    {"call":["get_setting_boolean","ray_color_ramps","ala"],"label":"per-object ray_color_ramps after set 1"}
  ],
  "expected": [
    false,
    true
  ]
}
