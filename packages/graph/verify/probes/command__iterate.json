{
  "id": "command__iterate",
  "feature": "iterate",
  "kind": "command",
  "probeKind": "state",
  "ops": [
    { "do": "load /home/tenplay/Documents/GitHub/10ground/sessions/map2/tenmol/packages/engine/test/dat/1tii.pdb, m" },
    { "do": "iterate all, b=999" }
  ],
  "checks": [
    { "call": ["count_atoms", "all"], "label": "total atoms loaded (baseline)" },
    { "call": ["count_atoms", "b>900"], "label": "iterate is read-only: b unchanged, expect 0" },
    { "call": ["iterate", "name CA", "1"], "label": "iterate returns per-atom count over CA subset" }
  ],
  "tol": 1e-3,
  "expected": [
    5684,
    0,
    712
  ]
}
