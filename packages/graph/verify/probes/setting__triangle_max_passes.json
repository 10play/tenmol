{
  "id": "setting__triangle_max_passes",
  "feature": "triangle_max_passes",
  "kind": "setting",
  "probeKind": "state",
  "ops": [
    {"do":"set triangle_max_passes, 8"}
  ],
  "checks": [
    {"call":["get_setting_int","triangle_max_passes"],"label":"global triangle_max_passes after set 8 (roundtrip)"}
  ],
  "expected": [
    8
  ]
}
