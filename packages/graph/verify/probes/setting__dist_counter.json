{
  "id": "setting__dist_counter", "feature": "dist_counter", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set dist_counter, 7, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","dist_counter"],"label":"dist_counter global default (no prior set)"},
    {"call":["get_setting_int","dist_counter","ala"],"label":"dist_counter per-object after set 7 (roundtrip)"}
  ],
  "expected": [
    0,
    7
  ]
}
