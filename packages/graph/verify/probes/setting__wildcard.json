{
  "id": "setting__wildcard",
  "feature": "wildcard",
  "kind": "setting",
  "probeKind": "state",
  "ops": [
    {"do": "load /home/tenplay/Documents/GitHub/10ground/sessions/map2/tenmol/packages/engine/test/dat/1tii.pdb, m"},
    {"do": "set wildcard, %, m"}
  ],
  "checks": [
    {"call": ["get_setting_text", "wildcard"], "label": "global default (unset) -> *"},
    {"call": ["get_setting_text", "wildcard", "m"], "label": "per-object set to % -> %"}
  ],
  "expected": [
    "*",
    "%"
  ]
}
