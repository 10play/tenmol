{
  "id": "setting__connect_mode", "feature": "connect_mode", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set connect_mode, 3, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","connect_mode"],"label":"global connect_mode default (no prior set)"},
    {"call":["get_setting_int","connect_mode","ala"],"label":"per-object connect_mode after set 3"}
  ],
  "expected": [
    0,
    3
  ]
}
