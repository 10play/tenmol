{
  "id": "setting__draw_frames",
  "feature": "draw_frames",
  "kind": "setting",
  "probeKind": "state",
  "ops": [
    {"do": "load /home/tenplay/Documents/GitHub/10ground/sessions/map2/tenmol/packages/engine/test/dat/1tii.pdb, m"},
    {"do": "set draw_frames, 1, m"}
  ],
  "checks": [
    {"call": ["get_setting_boolean", "draw_frames"], "label": "global default (unset) -> false"},
    {"call": ["get_setting_boolean", "draw_frames", "m"], "label": "per-object set to 1 -> true"}
  ],
  "expected": [
    false,
    true
  ]
}
