{
  "id": "setting__ambient", "feature": "ambient", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"set ambient, 0.5"}
  ],
  "checks": [
    {"call":["get_setting_float","ambient"],"label":"global ambient after set 0.5"}
  ],
  "tol": 1e-6,
  "expected": [
    0.5
  ]
}
