{
  "id": "setting__surface_mode", "feature": "surface_mode", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set surface_mode, 2, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","surface_mode"],"label":"global surface_mode default (no prior set)"},
    {"call":["get_setting_int","surface_mode","ala"],"label":"per-object surface_mode after set 2"}
  ],
  "expected": [
    0,
    2
  ]
}
