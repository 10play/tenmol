{
  "id": "setting__dot_density", "feature": "dot_density", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set dot_density, 3, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","dot_density"],"label":"global dot_density default (no prior set)"},
    {"call":["get_setting_int","dot_density","ala"],"label":"per-object dot_density after set 3"}
  ],
  "expected": [
    2,
    3
  ]
}
