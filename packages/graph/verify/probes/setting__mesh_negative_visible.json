{
  "id": "setting__mesh_negative_visible", "feature": "mesh_negative_visible", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set mesh_negative_visible, 1, ala"}
  ],
  "checks": [
    {"call":["get_setting_boolean","mesh_negative_visible"],"label":"global mesh_negative_visible default (no prior set)"},
    {"call":["get_setting_boolean","mesh_negative_visible","ala"],"label":"per-object mesh_negative_visible after set 1"}
  ],
  "expected": [
    false,
    true
  ]
}
