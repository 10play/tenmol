{
  "id": "setting__ray_opaque_background", "feature": "ray_opaque_background", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set ray_opaque_background, 1, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","ray_opaque_background"],"label":"global ray_opaque_background default (no prior set)"},
    {"call":["get_setting_int","ray_opaque_background","ala"],"label":"per-object ray_opaque_background after set 1"}
  ],
  "expected": [
    -1,
    1
  ]
}
