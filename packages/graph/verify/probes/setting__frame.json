{
  "id": "setting__frame", "feature": "frame", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set frame, 3, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","frame"],"label":"global frame default (no prior set)"},
    {"call":["get_setting_int","frame","ala"],"label":"per-object frame after set 3"}
  ],
  "expected": [
    1,
    3
  ]
}
