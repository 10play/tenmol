{
  "id": "setting__bg_gradient", "feature": "bg_gradient", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"set bg_gradient, 1"}
  ],
  "checks": [
    {"call":["get_setting_int","bg_gradient"],"label":"bg_gradient after set 1"},
    {"call":["get_setting_boolean","bg_gradient"],"label":"bg_gradient boolean after set 1"}
  ],
  "expected": [
    1,
    true
  ]
}
