{
  "id": "setting__antialias", "feature": "antialias", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set antialias, 2, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","antialias"],"label":"global antialias default (no prior set)"},
    {"call":["get_setting_int","antialias","ala"],"label":"per-object antialias after set 2"}
  ],
  "expected": [
    1,
    2
  ]
}
