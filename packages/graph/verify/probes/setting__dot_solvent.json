{
  "id": "setting__dot_solvent", "feature": "dot_solvent", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set dot_solvent, 1, ala"}
  ],
  "checks": [
    {"call":["get_setting_boolean","dot_solvent"],"label":"global dot_solvent default (no prior set)"},
    {"call":["get_setting_boolean","dot_solvent","ala"],"label":"per-object dot_solvent after set 1"}
  ],
  "expected": [
    false,
    true
  ]
}
