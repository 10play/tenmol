{
  "id": "setting__volume_mode", "feature": "volume_mode", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"set volume_mode, 2"}
  ],
  "checks": [
    {"call":["get_setting_int","volume_mode"],"label":"volume_mode after set 2 (set/get roundtrip)"}
  ],
  "expected": [
    2
  ]
}
