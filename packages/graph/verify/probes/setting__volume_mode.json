{
  "id": "setting__volume_mode", "feature": "volume_mode", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"load /home/tenplay/Documents/GitHub/10ground/sessions/map2/tenmol/packages/engine/test/dat/1tii.pdb, m"},
    {"do":"set volume_mode, 2, m"}
  ],
  "checks": [
    {"call":["get_setting_int","volume_mode"],"label":"volume_mode global DEFAULT (unset) == 0"},
    {"call":["get_setting_int","volume_mode","m"],"label":"volume_mode per-object after set 2 (roundtrip)"}
  ],
  "expected": [
    0,
    2
  ]
}
