{
  "id": "setting__label_font_id",
  "feature": "label_font_id",
  "kind": "setting",
  "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set label_font_id, 7, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","label_font_id"],"label":"label_font_id global default (expected 5)"},
    {"call":["get_setting_int","label_font_id","ala"],"label":"label_font_id per-object after set 7 (roundtrip)"}
  ],
  "tol": 0,
  "expected": [5, 7]
}
