{
  "id": "setting__label_digits", "feature": "label_digits", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set label_digits, 3, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","label_digits"],"label":"global DEFAULT label_digits (expect 1)"},
    {"call":["get_setting_int","label_digits","ala"],"label":"per-object label_digits after set 3 (roundtrip)"}
  ],
  "expected": [
    1,
    3
  ]
}
