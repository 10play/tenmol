{
  "id": "setting__cartoon_ring_mode", "feature": "cartoon_ring_mode", "kind": "setting", "probeKind": "state",
  "ops": [
    {"do":"fragment ala"},
    {"do":"set cartoon_ring_mode, 3, ala"}
  ],
  "checks": [
    {"call":["get_setting_int","cartoon_ring_mode"],"label":"global cartoon_ring_mode default (no prior set)"},
    {"call":["get_setting_int","cartoon_ring_mode","ala"],"label":"per-object cartoon_ring_mode after set 3"}
  ],
  "expected": [
    0,
    3
  ]
}
