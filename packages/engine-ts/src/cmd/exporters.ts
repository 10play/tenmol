/**
 * The `exporters` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerExporters`.
 */
import type { RegistrarCtx } from './registrar';
export function registerExporters(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
