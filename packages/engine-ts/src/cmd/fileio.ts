/**
 * The `fileio` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. See docs/engine-port-gaps.md.
 *
 * STUB: fill `registerFileio` with this subsystem's handlers.
 */
import type { RegistrarCtx } from './registrar';

export function registerFileio(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
