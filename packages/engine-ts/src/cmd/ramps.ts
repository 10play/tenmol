/**
 * The `ramps` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerRamps`.
 */
import type { RegistrarCtx } from './registrar';
export function registerRamps(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
