/**
 * The `maps` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerMaps`.
 */
import type { RegistrarCtx } from './registrar';
export function registerMaps(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
