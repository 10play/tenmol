/**
 * The `extras` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerExtras`.
 */
import type { RegistrarCtx } from './registrar';
export function registerExtras(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
