/**
 * The `props` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerProps`.
 */
import type { RegistrarCtx } from './registrar';
export function registerProps(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
