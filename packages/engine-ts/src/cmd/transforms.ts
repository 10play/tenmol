/**
 * The `transforms` command subsystem — filled in by its owning agent. Registers its
 * `cmd.*` handlers via the {@link RegistrarCtx}. See docs/engine-port-gaps.md.
 */
import type { RegistrarCtx } from './registrar';

export function registerTransforms(_ctx: RegistrarCtx): void {
  // (implemented by the transforms agent)
}
