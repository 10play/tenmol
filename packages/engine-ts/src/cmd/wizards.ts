/**
 * The `wizards` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerWizards`.
 */
import type { RegistrarCtx } from './registrar';
export function registerWizards(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
