/**
 * The `symmetry` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerSymmetry`.
 */
import type { RegistrarCtx } from './registrar';
export function registerSymmetry(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
