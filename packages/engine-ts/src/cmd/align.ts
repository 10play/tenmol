/**
 * The `align` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerAlign`.
 */
import type { RegistrarCtx } from './registrar';
export function registerAlign(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
