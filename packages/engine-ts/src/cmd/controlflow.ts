/**
 * The `controlflow` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerControlflow`.
 */
import type { RegistrarCtx } from './registrar';
export function registerControlflow(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
