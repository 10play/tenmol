/**
 * The `builder` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerBuilder`.
 */
import type { RegistrarCtx } from './registrar';
export function registerBuilder(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
