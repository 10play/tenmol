/**
 * The `display` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. See docs/engine-port-gaps.md.
 *
 * STUB: fill `registerDisplay` with this subsystem's handlers.
 */
import type { RegistrarCtx } from './registrar';

export function registerDisplay(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
