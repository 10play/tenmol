/**
 * The `misc` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerMisc`.
 */
import type { RegistrarCtx } from './registrar';
export function registerMisc(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
