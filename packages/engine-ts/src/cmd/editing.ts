/**
 * The `editing` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. See docs/engine-port-gaps.md.
 *
 * STUB: fill `registerEditing` with this subsystem's handlers.
 */
import type { RegistrarCtx } from './registrar';

export function registerEditing(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
