/**
 * The `movie2` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerMovie2`.
 */
import type { RegistrarCtx } from './registrar';
export function registerMovie2(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
