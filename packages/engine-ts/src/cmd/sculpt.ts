/**
 * The `sculpt` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. STUB: fill `registerSculpt`.
 */
import type { RegistrarCtx } from './registrar';
export function registerSculpt(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
