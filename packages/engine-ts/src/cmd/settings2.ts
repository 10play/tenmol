/**
 * The `settings2` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. See docs/engine-port-gaps.md.
 *
 * STUB: fill `registerSettings2` with this subsystem's handlers.
 */
import type { RegistrarCtx } from './registrar';

export function registerSettings2(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
