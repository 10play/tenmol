/**
 * The `measurement` command subsystem. Registers its `cmd.*` handlers via the
 * {@link RegistrarCtx}. See docs/engine-port-gaps.md.
 *
 * STUB: fill `registerMeasurement` with this subsystem's handlers.
 */
import type { RegistrarCtx } from './registrar';

export function registerMeasurement(_ctx: RegistrarCtx): void {
  // implemented per-subsystem
}
