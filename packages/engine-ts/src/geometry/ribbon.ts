/**
 * Mode-G geometry for the `ribbon` representation — a thin smoothed trace
 * through the Cα (and phosphate) backbone atoms. Emitted as `line` instances
 * (the viewport already draws that instance kind), one per spline segment,
 * coloured by the segment's atom colour.
 *
 * STUB: returns null (nothing drawn) until implemented.
 */

import type { RepBuilder } from './registry';

export const buildRibbonFrame: RepBuilder = () => null;
