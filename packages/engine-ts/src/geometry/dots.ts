/**
 * Mode-G geometry for the `dots` representation — the dot surface: points
 * sampled on each atom's (vdw+probe) sphere with buried points culled. Emitted
 * as `sphere` instances with radius 0 (the viewport draws those as a point
 * cloud). STUB: returns null until implemented.
 */
import type { RepBuilder } from './registry';
export const buildDotsFrame: RepBuilder = () => null;
