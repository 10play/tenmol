/**
 * Mode-G geometry for the `surface` representation — the solvent-excluded
 * molecular surface as a lit triangle mesh (indexed-mesh frame). Built from the
 * shared surface generator; drawn by the viewport's existing indexed-mesh path.
 * STUB: returns null until implemented.
 */
import type { RepBuilder } from './registry';
export const buildSurfaceFrame: RepBuilder = () => null;
