/**
 * Mode-G geometry for the `mesh` representation — the molecular surface as a
 * wireframe (line-strips over the surface triangulation). STUB: returns null.
 */
import type { RepBuilder } from './registry';
export const buildMeshFrame: RepBuilder = () => null;
