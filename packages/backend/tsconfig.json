{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Consumed as source (Vite / vitest / tsc paths); never built to dist.
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "noEmit": true,
    "types": []
  },
  "include": ["src/**/*.ts"]
}
