# @tenmol/web

**The React app shell.** The canonical documentation for this package now lives in the
project's Mintlify docs: [`docs/packages/web.md`](../../docs/packages/web.md).

That page is the single source of truth and is published to the docs site.
This stub keeps GitHub browsing (and package metadata) resolving to a pointer.
