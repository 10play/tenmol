> 251 components across 26 areas · 95 already on primitives · 156 to touch

# Modern UI conversion — component map

Generated inventory of every user-facing component, with its conversion status and how to browser-verify it. The modern theme (`[data-ui-theme=shadcn]` + `[data-appearance]`) is styled with Tailwind utility classes on components; all classic CSS lives in the low `legacy` cascade layer so those utilities win. Classic must stay pixel-exact.

Legend: **prim** = already imports the `ui/` primitive layer. **Status/verify** columns are filled as each is converted + browser-checked in dark AND light.

## shell

_The app window frame: the PyMOL-parity shell that hosts feature slots — menu-bar strip, 3D viewport, internal_gui block column, External GUI dock, status bar, overlay-panel launcher, connection modal and error fallbacks. Theme is switched by [data-ui-theme='shadcn'] + [data-appearance]; modern styling comes from Tailwind utilities on primitives (ui/Button.tsx) plus token rules in styles/shadcn.css, all winning over the classic 'legacy' cascade layer._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| AppShell (window/layout root) | `shell/AppShell.tsx` | other | ✓ | Container is already token-driven — no conversion needed. Only ensure any new chrome added here uses --pm-* tokens rather than literals, and keep the viewport deliberately dark in light mode (do NOT retint .shell__viewport). No Tailwind utilities required on t | Open http://100.71.244.15:3005/?theme=shadcn (dark) and add &appearance=light. Confirm the whole window recolors (bg, panels) except the central 3D viewport which must stay near-black in both. |
| Resize splitters (internal_gui gutter + ext-gui height) | `shell/AppShell.tsx` | control | — | Already modernized (hairline + accent-hover). No change needed. If ever converting to Tailwind, keep the ::after hairline approach (utilities can't easily draw a centered pseudo hairline) — leaving it as the §12 token rule is correct. | In modern theme, hover the seam between the viewport and the right block column — expect a thin accent line, not a solid bar. Requires the block column visible: enable 'internal_gui' from the header first. Drag to resize; double-click to collapse. Bottom split |
| ShellHeader (menu-bar strip / fallback chrome) | `shell/AppShell.tsx` | toolbar | ✓ | 1) Convert the three menubar toggles from Button+glyph to ToggleButton with pressed={dock.visible / internalGui / echoActions} and drop the '✓'/' ' string prefix, so modern shows the §4 accent 'on' fill (data-state=on). 2) Replace hardcoded .shell-chrome__note | Top strip of the app. In modern light+dark: click 'ext gui', 'internal_gui', 'echo actions' — each should flip to an accent-filled pressed state (after the ToggleButton change). Open the '◐ modern/classic' dropdown; toggle Sun/Moon. Confirm the 'menu bar not i |
| External GUI dock region | `shell/AppShell.tsx` | panel | — | Already token-driven and appearance-correct. No per-element Tailwind needed. Just ensure the console/quickbutton FEATURES inside (owned by features/, not shell) are on primitives — out of scope for shell, but the dock chrome here is done. | Toggle 'ext gui' on in the header to show the dock. Double-click it to get the title bar; use the title-bar buttons to move it Left/Right (flush, no shadow) and Float (frosted, shadowed, rounded). Check light+dark: docked = seamless, floating = frosted card. |
| ExtGuiTitleBar | `shell/AppShell.tsx` | toolbar | ✓ | 1) Swap the three area buttons and the visibility button to ToggleButton pressed={dock.area===area} instead of the hand-written className 'is-on', so state is data-state driven (consistent with §4). 2) Replace the single-letter B/L/R labels with lucide icons ( | Double-click the ext-gui panel to reveal this bar. In modern light+dark click B/L/R — the active area button should show the accent pressed fill; click float↔dock; click × to hide. Confirm icons render (modern only) and classic still shows plain text. |
| OverlayLayer (panel launcher pill + panel host) | `shell/AppShell.tsx` | overlay | ✓ | Already modernized (pill toolbar + icon toggles). No conversion required. Only nuance: the launcher gets BOTH the Tailwind GHOST utilities (from the primitive) and the §11 CSS — verify they don't fight (padding/radius). If they do, prefer the §11 rule and trim | Bottom-right floating pill (only appears when ≥1 overlay feature is installed). In modern light+dark, click each pill — icon + label, accent fill when its panel is open; the panel itself opens elsewhere (each brings its own positioning). |
| StatusBar | `shell/StatusBar.tsx` | other | — | 1) Fix the track: either replace the raw .statusbar__progress/.__progress-fill markup with the <ProgressBar> primitive (§12 already themes .progressbar → token bg + rounded), or add a shadcn.css rule setting .statusbar__progress { background:var(--sh-btn-fill) | Bottom status strip. Force a long op (or a session where cmd.get_progress()≥0) to show the bar, then switch to &appearance=light — the track must NOT be a black chip. Check separators/dim text legibility in light. |
| ConnectionOverlay | `shell/ConnectionOverlay.tsx` | dialog | — | Largest conversion in the folder. 1) Replace the raw <input> with <TextInput> (data-slot='input' → §6 themes it for both modes). 2) Replace the three raw <button className="quickbutton"> with <Button variant='quick'> (gains MODERN_VARIANT FLAT fill + is-on rul | It is ON SCREEN RIGHT NOW (the api/bridge at 127.0.0.1:8005 is unreachable). Load http://100.71.244.15:3005/?theme=shadcn then &appearance=light: today the token input, the `pnpm dev:bridge` code chip, and the health box are black boxes with invisible text in  |
| ErrorBoundary failure box | `shell/ErrorBoundary.tsx` | other | — | Retint via tokens + primitive. 1) Add a shadcn.css block (or Tailwind classes gated to modern) turning .feature-failed into a soft-danger card: background:color-mix(in srgb, var(--sh-danger) 12%, var(--pm-panel)); border-color:color-mix(in srgb, var(--sh-dange | Force a feature to throw (temporarily break a feature's register.ts, or throw in its Panel) so the boundary renders. In modern light+dark, confirm it's a soft danger card (not a black-red block) with a themed retry button; classic must be unchanged. |
| FeatureSlot placeholders (absent / loading) | `shell/FeatureSlot.tsx` | other | — | Tokenize the hardcoded values: .feature-absent color:#4a4f55 → var(--pm-text-dim); .feature-absent__owner border #2b2f33 → var(--pm-line), color #5b6167 → var(--pm-text-dim), or replace the owner span with the <Badge> primitive for a themed chip. Keep the mono | Rare states. 'absent' shows when a registry slot declares an `absent` note but its feature dir isn't installed; 'loading…' flashes while a lazy overlay/panel module is fetched (open a panel from the launcher on a cold load). Check the owner chip is legible in  |

## apps/web/src/features/menubar

_The PyMOL menu bar feature: a top strip of 11 hand-rolled dropdown menus (File…Help) driven by generated menu data, with live check/radio state, cascading fixed-positioned submenus, an Open Recent dynamic list, an About dialog, and a modal host for the Colors/Shortcuts/Scenes toolkit dialogs. Entirely hand-written BEM DOM; nothing imports the ../../ui primitive layer. Classic + modern looks are both driven purely by CSS (global.css base in the `legacy` layer, menubar.css additions, and shadcn.css `[data-ui-theme='shadcn']` overrides), so "modernizing to Tailwind" means adding modern-gated utility classes on the elements and retiring the matching shadcn.css blocks, plus fixing two dialogs that have NO modern override and hardcode dark colors that break in light mode._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| MenuBar (top strip + triggers) | `features/menubar/MenuBar.tsx` | toolbar | — | 1) Read useTheme() once (`const modern = useTheme() === 'shadcn'`). 2) On each trigger button replace the string className with cn('menubar__item', open===index && 'is-open', modern && 'rounded-[6px] border border-transparent bg-transparent px-2.5 py-1 text-[1 | Open http://100.71.244.15:3005. Toggle modern via the shell header theme switch (or append ?theme=shadcn). Confirm the 11 triggers render as flat ghost buttons (dim text, no border) that fill on hover and take the soft-accent tint when open. Switch appearance  |
| MenuList (cascading popup + check/radio rows) | `features/menubar/MenuList.tsx` | menu | — | 1) Thread a `modern` boolean (useTheme) into MenuList. 2) Add modern-gated utilities to each element so they override the legacy layer and let the shadcn.css block be retired: container cn('menu', nested&&'menu--sub', flip&&'menu--flip', modern && 'rounded-[8p | Modern theme: open Display ▸ Stereo Mode (a 4-level cascade) — confirm the popup is a frosted, rounded, soft-shadow card, rows are 26px tall with rounded accent-fill hover, and nested submenus open to the side without being clipped by the parent's scrollbar. O |
| DynamicList (Open Recent body) | `features/menubar/MenuList.tsx` | menu | — | Covered by the MenuList conversion — because it reuses .menu / .menu--sub / .menu__row, applying the same modern-gated utilities there restyles this automatically. Only extra step: pass the same `modern` flag down (or lift the shared row/list class computation | Modern theme: File ▸ Open Recent — confirm the recent-file list renders in the same frosted rounded popup with rounded accent-hover rows, and that a long history is pulled up/clamped to stay on screen. With no history, confirm the disabled 'no recent files' ro |
| AboutDialog (Help ▸ About PyMOL) | `features/menubar/AboutDialog.tsx` | dialog | — | Best path: replace the hand-rolled markup with the ui primitives — <Dialog open>/<DialogContent> (frosted, rounded, token bg + light/dark aware overlay) for the shell, keep the mono .about__body lines as children, and render the OK action as <Button variant='b | Modern DARK: Help ▸ About PyMOL — dialog should be a frosted rounded card, the OK button a subtle token-filled button. Switch to LIGHT (?appearance=light) and reopen: the card must be white with legible dark text and the OK button must be a light button with a |
| ToolkitDialogHost (Colors / Shortcuts / Scenes modal host) | `features/menubar/toolkitHooks.tsx` | dialog | — | 1) Ideally re-shell on the ui Dialog primitive (<DialogContent> gives frosted/rounded/token bg + a light-safe overlay and a built-in lucide X close), rendering TITLES[dialog] via <DialogTitle> and the lazy body as children; keep the special-case that renders S | Modern DARK: Edit ▸ Colors… (or Setting menu → a toolkit leaf) — the modal should be a frosted rounded sheet with a token title bar and a hover-highlighted close control; the color table scrolls inside the body. Switch to LIGHT and reopen: card must be white,  |

## features/pymol-menu

_The pymol-menu feature renders the app's single PyMOL right-click pop-up (context menu overlay): MenuHost is the always-mounted service host (also installs the volume leaf-hook bridge), PopupMenu is the floating menu itself with a heading, separators, non-clickable section titles, and clickable leaf-item buttons. It is the one bespoke menu NOT yet given a modern override — its .pymenu* BEM classes have no [data-ui-theme='shadcn'] rule anywhere (the .popup-menu selector in shadcn.css is dead; the real class is .pymenu), so in modern dark mode it only inherits token VALUES while keeping classic geometry, and it ships a hardcoded pure-black box-shadow that is wrong in light mode._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| MenuHost | `features/pymol-menu/MenuHost.tsx` | service | — | No change needed. Leave as a pure host. Do NOT move the installVolumeMenuBridge effect or the PopupMenu render — the file comment documents that registering the leaf hook must happen from an always-mounted component, so any refactor must preserve that it rende | Not directly visible. Confirm indirectly: the pop-up appears at all (see PopupMenu) and the volume 'panel' leaf opens the React volume editor rather than echoing cmd.volume_panel to the console. |
| PopupMenu (pop-up container .pymenu) | `features/pymol-menu/PopupMenu.tsx` | menu | — | PopupMenu must stay a pure renderer whose .pymenu* class names + data-testid + role are pinned (PopupMenu.dom.test.tsx drives it directly), so ADD Tailwind utilities gated to modern, never rename. Add `import { useTheme } from '../../ui'` and `const modern = u | Front is up (HTTP 200) at http://100.71.244.15:3005. Open modern light via URL: http://100.71.244.15:3005/?theme=shadcn&appearance=light (also try &appearance=dark, and ?theme=classic which must stay pixel-exact). To raise the pop-up: right-click (button 2) th |
| Menu leaf item (.pymenu__item) | `features/pymol-menu/PopupMenu.tsx` | control | — | Gate on the same `modern` flag and add to the button className: modern && 'h-[26px] rounded-[var(--sh-r-sm)] px-2.5 text-[12.5px] text-[var(--pm-text)] hover:bg-[var(--pm-accent)] hover:text-[var(--sh-accent-text)]'. The hover: utilities sit in @layer utilitie | Same route as PopupMenu (right-click the Mouse Config block). Hover the rows: in modern each row should light up as a rounded inset accent pill with white text; in light mode confirm the accent fill and white text remain legible. Click a leaf — the menu closes |
| Menu heading (.pymenu__head) | `features/pymol-menu/PopupMenu.tsx` | other | — | Low priority / optional. If touched, gate on modern and normalise to the modern section-header token recipe: modern && 'border-b border-[var(--pm-line)] px-2.5 pb-1 pt-[3px] text-[10px] font-semibold uppercase tracking-[0.08em] text-[var(--pm-text-dim)]'. No c | Same route; the 'Mouse Config' heading sits at the top of the pop-up. Confirm it stays a muted uppercase tracked label with a hairline underline in both dark and light modern. |
| Menu section title (.pymenu__title) | `features/pymol-menu/PopupMenu.tsx` | other | — | Gate on modern and override the green to the neutral dim label used by the modern menus: modern && 'px-2.5 py-[3px] text-[10px] font-semibold uppercase tracking-[0.08em] text-[var(--pm-text-dim)]'. This replaces var(--pm-caption) green with --pm-text-dim so se | Only shows when a menu contains code-2 rows (the Mouse Config menu does). Confirm section captions render as muted grey uppercase labels, not green, in modern light AND dark. |
| Menu separator (.pymenu__sep) | `features/pymol-menu/PopupMenu.tsx` | other | — | Lowest priority. If normalising, gate on modern: modern && 'mx-2 my-[5px] bg-[var(--pm-line)]' to match .menu__sep insets. No light-mode fix required. | Same route; separators between menu groups should be a single hairline in var(--pm-line), correctly faint on both the dark and light modern surface. |

## apps/web/src/features/objects

_The Executive "names list" object panel and its PyMOL-style right-click popup menu — real-DOM reproductions of CExecutive::draw and PopUp.cpp, fed live from the bridge. Two rendering files (ObjectPanel.tsx, RowMenu.tsx) styled by objects.css + global.css objrow rules (both @layer legacy) with a partial modern override in shadcn.css §9; RowMenu has NO modern styling and breaks in light mode._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ObjectPanel | `features/objects/ObjectPanel.tsx` | panel | — | Optionally render the outer div through the ui Panel primitive (or add data-slot='panel') so modern gets the frost/border card. Keep the load-bearing min-height floor and overflow rules exactly (they are guarded by e2e smoke + test_p11_layout). Do NOT convert  | Open http://100.71.244.15:3005/?theme=shadcn&appearance=light (and again with appearance=dark). The panel is the Executive block filling the bottom of the right-hand .internal-gui column. If empty, type `fragment ala` in the console to add rows. Check the pane |
| Panel header + feed-source badge | `features/objects/ObjectPanel.tsx` | toolbar | — | Replace the hardcoded #5b6167 on .objpanel__head-note with token color:var(--pm-text-dim) (or render it as the ui Badge primitive). In the component add `const modern = useTheme()==='shadcn'` and gate a modern class onto the note: cn('objpanel__head-note', mod | Same URL as ObjectPanel, both appearances. Confirm the 'Objects' header reads as a muted uppercase sans label and the panel/pushed/polled badge is legible (not washed out) in LIGHT mode. Hover the badge to see the 'row source:' tooltip. |
| Empty-state + error line | `features/objects/ObjectPanel.tsx` | other | — | Tokenize in objects.css: .objpanel__empty → color:var(--pm-text-dim); .objpanel__error → color:var(--sh-danger). OR gate modern classes in JSX: empty → cn('objpanel__empty', modern && 'text-[var(--pm-text-dim)]'); error → cn('objpanel__error', modern && 'text- | Empty: open the app with no objects loaded (fresh session) in LIGHT and DARK — confirm the hint text is legible. Error: temporarily disconnect the bridge (stop api) so the snapshot loop faults, and confirm the red error line uses the calm danger token in light |
| Object row | `features/objects/ObjectPanel.tsx` | other | — | Keep .objrow height/gap untouched (18px is guarded). No Tailwind needed on the wrapper itself beyond what tokens already give; state classes already resolve through tokens. Focus modern work on the three child controls below. If desired, add modern rounded row | Load a few objects (`fragment ala`, `fragment gly`), a group (`group g1, ala gly`) and a selection. In LIGHT and DARK confirm enabled vs disabled vs cloaked (enabled child inside a disabled group) rows are distinguishable, left-drag paints a visible pressed ba |
| Group expand/collapse toggle | `features/objects/ObjectPanel.tsx` | control | — | Either (a) render via the ui IconButton primitive with a lucide ChevronRight/ChevronDown (variant that maps to a bare/control class) so modern gets a real disclosure icon while classic keeps the +/- text, or (b) gate a modern class: cn('objrow__group', modern  | Create a group (`group g1, ala gly`) so a +/- toggle appears. In LIGHT and DARK click it to open/collapse and confirm the affordance is visible on the light panel and the icon/glyph is legible. |
| Object name button | `features/objects/ObjectPanel.tsx` | control | — | Base fill/border/hover already handled by shadcn.css §9 — optionally move those four rules onto the component as gated Tailwind (cn('objrow__name', modern && 'rounded-[6px] border border-[var(--sh-btn-border)] bg-[var(--sh-btn-fill)] text-[var(--pm-text)]') pl | Load a colored molecule (`fragment ala; color red, ala`) and an object with a caption/state. In LIGHT mode confirm the name text and caption stay readable on the accent-soft enabled fill (the object's own color must not vanish). Exercise click-toggle, shift, c |
| Op toggle buttons (A / S / H / L / C / M) | `features/objects/ObjectPanel.tsx` | control | — | Decide the modern language: either keep PyMOL's per-op hues (they are semantic) but calm them, or unify to token surfaces. Concretely, in shadcn theme override the op tokens to muted/desaturated values and swap the C rainbow for a single tasteful swatch or a s | On any molecule row, in LIGHT and DARK: confirm the A/S/H/L/C/M buttons read as a coherent control strip (not clashing neon), the C button isn't a jarring rainbow on the light panel, an is-inert button (e.g. L on a map object — `load` a map or `isomesh`) looks |
| RowMenu (popup) | `features/objects/RowMenu.tsx` | menu | — | This is the top-priority fix. Do NOT rebuild on Radix — restyle in place. In the component: const modern=useTheme()==='shadcn'; className={cn('rowmenu', internalGuiMode!==0 && 'is-light', modern && 'rounded-[var(--sh-r)] border border-[var(--sh-line-strong)] b | Open the app at ?theme=shadcn&appearance=light, load a molecule, click its A (Actions) button. The popup MUST become a light frosted card with dark legible text — today it is a dark box with invisible dark text. Re-check at appearance=dark. Hover a submenu arr |
| Menu leaf row (+ RepTick / Coded) | `features/objects/RowMenu.tsx` | control | — | Fix as part of the RowMenu restyle. Replace the hardcoded .rowmenu__row.is-rep-on color:#fff with color:var(--pm-text-bright) (or drop the white and rely on font-weight for 'on'). Gate a modern class: cn('rowmenu__row', modern && 'rounded-[var(--sh-r-sm)] h-[2 | Open a Show (S) menu on a molecule so rep leaves render with ✓ check marks. At appearance=light after the RowMenu fix, confirm every leaf label is legible, the ✓/· ticks use the accent, an 'on' rep leaf is distinguishable, hover shows the accent fill with legi |
| SubMenu (cascading submenu) | `features/objects/RowMenu.tsx` | menu | — | Restyle the nested .rowmenu__panel with the SAME modern token treatment as the root RowMenu card (frost bg-[var(--sh-panel-frost)], border-[var(--sh-line-strong)], rounded-[var(--sh-r)], shadow-[var(--sh-pop)], backdrop blur) — either via a shadcn.css [data-ui | Open an Actions (A) or Color (C) menu that has ▸ submenus (e.g. presets/color palettes). At appearance=light hover a ▸ row: the nested panel must open as a matching light frosted card, side-flip correctly near the viewport edge, and its deeper levels stay them |
| Menu title + separator | `features/objects/RowMenu.tsx` | other | — | Tokenize: .rowmenu__title → color:var(--pm-text-dim); ensure the separator reads on light by using --sh-line-strong (or color-mix bumping --pm-line opacity) instead of the faint --pm-line. Gate under modern if done in CSS: [data-ui-theme='shadcn'] .rowmenu__se | Open a menu that has section titles and separators (Actions menu). At appearance=light confirm separator hairlines are visible on the light card and title rows are legible-but-muted; re-check dark. Trigger a lazy submenu to see the 'resolving…' title render co |

## apps/web/src/features/viewport

_The always-mounted 3D viewport slot: a framework-free @tenmol/viewport canvas host (WebGL/Mode-P pixel stream + Mode-G three.js geometry) with a bespoke React diagnostics HUD overlaid top-left; the rest of the dir (transport.ts, devFrames.ts, devFixtures.ts, register.ts, index.ts) is non-UI plumbing/dev switches._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ViewportPanel (root .viewport container + .viewport__host) | `features/viewport/ViewportPanel.tsx` | panel | — | Lowest priority — the black stage is right in both appearances. Optional token alignment only: add useTheme() and gate one class on the host, className={cn('viewport', modern && 'bg-[var(--pm-bg-viewport)]')}, so the black comes from the token not a literal #0 | Open http://100.71.244.15:3005/?theme=shadcn (modern dark) — the center 3D area fills the shell and is black/near-black. Flip to light via the header Sun/Moon (data-testid='appearance-toggle') or ?appearance=light: the 3D stage must STAY dark (only the chrome  |
| Viewport diagnostics HUD (overlay panel: stat lines + warn lines) | `features/viewport/ViewportPanel.tsx` | overlay | — | Add const modern = useTheme()==='shadcn' and gate Tailwind on the outer div: className={cn(`viewport__hud${hudOpen?'':' viewport__hud--closed'}`, modern && 'rounded-lg border border-white/10 bg-[rgb(8_10_14/72%)] backdrop-blur-md shadow-[var(--sh-pop)]'). CRIT | ?theme=shadcn: the HUD sits top-left over the 3D scene showing the Mode P / Mode G stat lines (in the session screenshot it overlaps a green wizard prompt at the very top-left). Flip appearance to light (Sun/Moon in header): the HUD MUST remain a dark transluc |
| HUD collapse toggle button (× / i) | `features/viewport/ViewportPanel.tsx` | control | — | Cleanest: replace with the primitive IconButton (variant='bare' or 'control') so it inherits the modern ghost look, but override sizing to stay 20px: <IconButton variant='bare' className={cn('viewport__hud-toggle', modern && 'rounded-md text-slate-400 hover:bg | ?theme=shadcn: click the × in the HUD's top-right — it collapses to a small dot/'i' badge; click 'i' to re-expand. Confirm rounded hover highlight and legible glyph in BOTH dark and light appearance. |
| HUD rep-mode P/G toggle cluster (.viewport__hud-reps) | `features/viewport/ViewportPanel.tsx` | toolbar | — | Best: swap each raw <button> for the ToggleButton primitive so 'on' automatically maps to var(--pm-accent) via data-[state=on] (kills the #1e88e5 literal): <ToggleButton pressed={effective==='pixel'} variant='bare' className='h-[18px] min-w-[18px] px-0 text-[1 | Load a molecule (or run with ?viewportFixtures / any object present) so reps show; ?theme=shadcn. In the HUD's rep row, click a rep's 'G' then 'P': the active pill must fill with the SAME indigo as a selected object row / the movie play-button — not the legacy |
| FileDropTarget (+ PluginDialogHost) — mounted-for-life service | `features/files/FileDropTarget.tsx` | service | — | No action in the viewport conversion. When converting the files feature, ensure PluginDialogHost's dialog uses the Dialog primitive / token surface so it is themed + light-mode correct; nothing to do in features/viewport. | Not visually part of the viewport. To exercise: drag a .pdb onto the window, or press Ctrl+O, and confirm the file loads (an object appears in the OBJECTS panel and the console echoes the load) — behavior only, no viewport-specific visual to check. |

## apps/web/src/features/console

_The External GUI console feature: an output toolbar + feedback log + PyMOL command line + quick-button grid (the docked dock), plus three portalled, pixel-faithful PyMOL viewport overlays (in-viewport ortho console, busy box, rubber-band marquee). Reach the modern look with the ThemeToggle in the shell header or the URL params ?theme=shadcn&appearance=light|dark (theme.tsx resolves URL > localStorage). The ext-gui dock sits at the bottom of the shell by default (extGuiDock.ts); the three overlays portal over .shell__viewport._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ConsolePanel (External GUI dock wrapper) | `features/console/ConsolePanel.tsx` | panel | — | Little to do on the wrapper itself. When migrating section-8 rules from shadcn.css to Tailwind (task #10), gate them behind useTheme()==='shadcn' on the .extgui / .extgui--float containers rather than here. Main work is in the child ConsoleToolbar (below). Con | Open http://100.71.244.15:3005, switch to modern (ThemeToggle in the header, or ?theme=shadcn). The ext-gui dock is the bottom strip: toolbar on top, feedback log, then the PyMOL> command line, with the quick-button column beside/above them. Toggle appearance  |
| ConsoleToolbar (.console__bar output toolbar) | `features/console/ConsolePanel.tsx` | toolbar | — | Replace the raw buttons with the primitive: <Button variant="consoleBar"> for A-/A+/clear/splash (MODERN_VARIANT.consoleBar already provides the FLAT h-[22px] chip), and <ToggleButton variant="consoleBar" pressed={orthoVisible}/{follow}> for 'ortho' and 'follo | Modern theme: the toolbar is the top strip labelled 'OUTPUT' with the line count and the ortho/follow/A-/A+/clear/splash controls plus the font picker. Click 'ortho' and 'follow' and confirm the accent on-fill. Switch to ?appearance=light and confirm the count |
| Font family <select> (.console__bar-font) | `features/console/ConsolePanel.tsx` | form | — | Swap to the <Select> primitive from ../../ui so it renders data-slot='select' and inherits the modern field look (radius, --sh-btn-fill, --sh-btn-border, padding) that already works in dark AND light. Keep the className="console__bar-font" so classic is byte-i | Modern + light: open the font dropdown in the toolbar; confirm the closed control matches the other modern fields (not a tiny legacy box) and the popup list is legible in both appearances; pick 'Monaco' and confirm the feedback pane re-renders in that family. |
| FeedbackLog (.feedback output pane) | `features/console/FeedbackLog.tsx` | panel | — | Largely done. Optional polish: (1) tokenize the severity palette into semantic vars (e.g. error->--sh-danger, add a --sh-warning) so the colours are design-system-managed rather than magic hexes, while keeping them dark-surface values. (2) Convert .feedback__m | Modern theme: run a valid command and a bad one to produce prompt/info/error lines; note client-origin lines (italic dim) from button presses; scroll the pane past 1200 lines to reveal the .feedback__more 'show N earlier lines' link. Toggle ?appearance=light a |
| CommandLine (.cmdline PyMOL> prompt + input) | `features/console/CommandLine.tsx` | form | — | Optional primitive adoption: swap the raw <input className="cmdline__input"> for <TextInput className="cmdline__input"> — BUT note it then also matches shadcn.css section 6 [data-slot='input'], which would fight the section-7 .cmdline__input rule; either keep  | Modern theme: the command line is the bottom strip with the 'PyMOL>' prompt; type and Enter, press Tab to complete. Stop the api (./stop api) to see the label go strikethrough/offline. Toggle ?appearance=light: confirm the 'PyMOL>' prompt reads on the light ba |
| QuickButtons (.quickbuttons grid + progress row) | `features/console/QuickButtons.tsx` | toolbar | ✓ | Two small fixes: (1) Give the TODO state a real modern look — replace reliance on the legacy .quickbutton--todo with gated Tailwind on the todo branch (e.g. border-dashed text-[var(--pm-text-dim)] opacity-70) so an unbuilt button is visibly unbuilt in modern t | Modern theme: hover the grid to confirm FLAT chips with lucide icons; the movie row shows icon-only transport buttons. Trigger a long op (type 'ray 2000,2000' async) to make the progress row + red Abort appear, then vanish when done. Before features/render reg |
| BusyOverlay (.ortho-busy in-viewport busy box) | `features/console/BusyOverlay.tsx` | overlay | — | Recommend LEAVING pixel-faithful (it reproduces a GL overlay and matches classic). It is not really a design-system target. If a modern variant is ever wanted, gate it strictly behind [data-ui-theme='shadcn'] so ONLY modern adopts frost/rounded/accent-bar (bac | Trigger a long synchronous-ish op (e.g. 'ray 3000,3000') to make the box appear top-left inside the viewport. Confirm it looks identical in classic and modern and in both appearances (it should stay a black box on the dark scene). Confirm it disappears when th |
| OrthoConsole (.ortho in-viewport command console) | `features/console/OrthoConsole.tsx` | overlay | — | Recommend LEAVING as pixel-faithful — these are literal PyMOL colour constants over the dark scene, not chrome. Not a design-system target and classic must stay identical. The only element that could conceivably get gated Tailwind is the .ortho__hidden helper  | Click the toolbar 'ortho' button to show it; run 'set internal_feedback, 3' to draw the black band; click the band to focus and type (caret follows). Confirm classic and modern render the same over the dark scene and it stays light-blue-on-black in both appear |
| OrthoLoopRect (.ortho-loop rubber-band marquee) | `features/console/OrthoLoopRect.tsx` | overlay | — | Recommend LEAVING pixel-faithful (matches PyMOL and classic). Optionally, ONLY under [data-ui-theme='shadcn'], a modern accent-tinted marquee could be gated (border-[var(--pm-accent)]) — cosmetic and departs from parity, so treat as optional. No light-mode fix | In a box-selection mouse mode (e.g. set button mode so a drag draws a marquee), drag on the viewport and confirm the rectangle tracks the pointer; test add (solid), subtract (dashed) and set (dotted) via the modifier keys/buttons. Confirm white-on-dark in clas |

## apps/web/src/features/movie

_The Movie feature (internal-gui right-column block, registry order 30): a scrollable panel with a nine-button transport bar, frame seek slider, four tabs (timeline canvas / editors / cascading menu / sweep+camera), a right-click motion context menu, and an export modal. Only the transport bar is modernized so far (shadcn.css §10); all other surfaces still render via @layer legacy movie.css with hardcoded dark colors that break in light mode._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| MoviePanel (root + header/clock/export trigger + error banners) | `features/movie/MoviePanel.tsx` | panel | — | Add `const modern = useTheme()==='shadcn'`. Error banner: gate `modern && 'rounded-[6px] border border-[var(--sh-danger)]/40 bg-[color-mix(in_srgb,var(--sh-danger)_14%,transparent)] text-[var(--sh-danger)] px-2 py-1'` via cn() so it overrides the legacy #2a1c1 | Open http://100.71.244.15:3005 with ?theme=shadcn; the Movie block is the 3rd panel in the right-hand internal-gui column (below Objects + Wizards, above Scenes). Trigger an error (e.g. temporarily fail the panel endpoint) and toggle ?appearance=light — confir |
| Frame seek slider | `features/movie/MoviePanel.tsx` | control | — | Swap the raw range input for the Slider primitive from '../../ui' (native behaviour preserved, adds data-slot='slider' picked up by shadcn.css §6). Keep min/max/value/disabled/aria-label/onChange verbatim. Optionally add a touch more vertical padding in modern | With a movie defined (run `mset 1 x30` in the command line so nframes>0) the slider under the transport bar enables; drag it in ?theme=shadcn light AND dark and confirm the accent-colored fill and correct scrubbing. |
| Tab bar (timeline / edit / menu / sweep) | `features/movie/MoviePanel.tsx` | toolbar | — | Convert to the ToggleButton primitive (variant 'bare', pressed={tab===name}); its data-state='on' fill uses --sh-accent-text (correct white), fixing the #0e0f11 text automatically. Minimal alternative: gate Tailwind on the raw buttons — base `modern && 'rounde | ?theme=shadcn light+dark: click each tab; confirm the active tab label is legible (white on accent, not dark-on-purple) and hit targets grew to modern size. |
| Sweep + Camera tab | `features/movie/MoviePanel.tsx` | form | — | Replace action buttons with Button (variant 'bare') and the four sweep_mode buttons + rocking button with ToggleButton (pressed=...) so on-state uses --sh-accent-text. Replace the <textarea> with the TextArea primitive (data-slot='textarea' -> shadcn.css §6 re | ?theme=shadcn 'sweep' tab: toggle a sweep mode and confirm the lit button is accent + white text in both appearances; in light mode confirm the camera textarea is a light field (not black); click get_view to populate it. |
| TransportBar (nine-button transport) | `features/movie/TransportBar.tsx` | toolbar | ✓ | Essentially DONE. Optional polish: add lucide icons to seq/rock/full for consistency; switch the seq + rock buttons from Button+manual `is-on` class to ToggleButton (pressed=...) for semantic state. No light-mode fix needed. | ?theme=shadcn dark AND light: the bar is a rounded segmented control; click mv-play and confirm accent fill + white glyph; toggle mv-seq/mv-rock and confirm accent fill; confirm lucide icons on the six arrow buttons. |
| MovieTimeline (canvas strip + label gutter + footer) | `features/movie/MovieTimeline.tsx` | panel | — | Treat the canvas as a stage: replace `.mvtl__canvas { background:#101113 }` with var(--pm-bg-viewport) (pinned dark in both appearances) so it matches the viewport, and round it in modern (`modern && 'rounded-[6px]'`). Leave paint() colors as-is (tuned for a d | Define a movie (`mset 1 x30`), open the 'timeline' tab; in ?theme=shadcn light+dark confirm the strip reads as a dark stage matching the viewport, the playhead + green scene pins are visible, and footer hint text is legible. Right-click a row cell to open the  |
| MovieEditors (edit tab: mset/mdo/mview/range) | `features/movie/MovieEditors.tsx` | form | — | Swap the two text <input>s -> TextInput, the two number inputs -> TextInput (keep .mvedit__num width class), the <select> -> Select primitive (data-slot='select' -> modern field), and all action buttons -> Button (variant 'bare'). shadcn.css §6 then repaints t | ?theme=shadcn light: open the 'edit' tab; confirm all text/number fields + the select are light with a subtle border (not black) and buttons use the modern flat style. Type an invalid mset spec (e.g. 'x') and confirm the preview error is a proper red. Repeat i |
| MovieMenu / MovieMenuTree (menu tab) | `features/movie/MenuViews.tsx` | menu | — | Gate modern Tailwind on .mvmenu__btn: taller rows + radius + `hover:bg-[var(--pm-accent)] hover:text-[var(--sh-accent-text)]` to fix the #0e0f11 hover text; keep the ✓/● marks and arrows. Bigger option (aligns with task WP-8): rebuild on the Radix DropdownMenu | ?theme=shadcn light+dark: open the 'menu' tab; hover rows and confirm highlight text is white-on-accent (not dark). Expand a submenu, toggle a check item and confirm the ✓ reflects the live polled setting. |
| MotionMenu (right-click camera/object context menu) | `features/movie/MenuViews.tsx` | overlay | — | Gate modern styling on .mvpopup: replace the hardcoded shadow with var(--sh-pop), add radius var(--sh-r) + optional frost (--sh-panel-frost + backdrop-filter) to match the app's other floating menus (shadcn.css §5). Fix the shared .mvmenu__btn hover text (see  | Define a movie, open the timeline tab, right-click a track cell (or ctrl+shift right-drag for the all-rows column menu) to open the popup at the cursor; in ?theme=shadcn light confirm a soft shadow + rounded card (not a hard black box) and legible hover rows;  |
| ExportDialog (export movie modal) | `features/movie/ExportDialog.tsx` | dialog | ✓ | Give the format radios accent theming — add a className that hooks a modern rule setting `accent-color: var(--pm-accent)` (or wrap each in a labeled control). Repoint the .mvexport shadow to var(--sh-pop) + add var(--sh-r) radius and optional frost gated to mo | Click 'export' in the Movie panel header to open the modal; in ?theme=shadcn light+dark confirm fields are light-themed, the format radios use the accent color, the card has a soft (not black) shadow, disabled formats are dimmed, and error/warning lines are le |

## apps/web/src/features/scenes

_The Scene bin feature (region internal-gui): a scrolling panel that contains the PyMOL scene-buttons overlay strip, a scene table (thumbnail/name/message/stores/reorder/update/delete), the flat Scene "menu" of actions, a right-click scene_menu popup, and a subordinate named-camera Views list. Primitives already modernize themselves; the scene-specific BEM containers/raw elements have NO shadcn.css override, so they keep hardcoded dark hex and break in light mode._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ScenePanel (root container) | `features/scenes/ScenePanel.tsx` | panel | ✓ | Add `const modern = useTheme() === 'shadcn'` at the top and thread modern-gated Tailwind onto the wrapper via cn('scpanel', modern && 'font-[var(--pm-font-ui)] text-[11px]'). Do NOT change the flex/overflow geometry (it is load-bearing per the big comment abou | Open http://100.71.244.15:3005/?theme=shadcn&appearance=light and again with appearance=dark. The Scene bin is a panel in the right-hand internal-gui dock. Confirm the panel chrome uses the sans UI font and that the panel background matches the surrounding mod |
| Scene panel header / toolbar | `features/scenes/ScenePanel.tsx` | toolbar | ✓ | Give the head buttons a compact modern footprint: pass a smaller sizing via className gated to modern, e.g. cn('scpanel__btn', modern && 'h-6 px-2 text-[11px]') on each Button so twMerge overrides FLAT's h-7 px-3 text-[12px]; or introduce a dedicated compact B | In modern light and dark, confirm the +, ‹, ›, sort buttons sit inside the header band without clipping, use the modern flat button look, and hover to the modern hover fill (not the dark #5d6165). Click '+' to store a scene and confirm the strip + a table row  |
| Scene-buttons overlay strip | `features/scenes/ScenePanel.tsx` | overlay | — | Add `const modern = useTheme()==='shadcn'` and gate Tailwind arbitrary-value utilities that read tokens (keep the inline absolute-position/size styles untouched — geometry is engine-driven). Strip: cn('scbar', ..., modern && 'bg-[var(--pm-panel-alt)]'). Button | Store 3-4 scenes via the header '+'. In modern light: the strip must read as a light tokenised toolbar (no dark #101113 slab), buttons legible, the current scene tinted with accent-soft, and a dragged button flashing accent. Repeat in dark. Scroll the strip (w |
| Scene bin table + row | `features/scenes/ScenePanel.tsx` | panel | ✓ | Gate a `modern` flag and: (1) .scrow.is-current -> cn(..., modern && 'bg-[var(--sh-accent-soft)]'); (2) .scrow.is-dropzone -> modern && 'outline-1 outline-dashed outline-[var(--pm-accent)]' (define/route --accent -> --pm-accent); (3) .scrow__handle -> modern & | With several scenes stored, in modern light+dark: recall a scene and confirm the current row highlights with a soft accent tint (not the dark #22252b bar); click a row (is-selected accent outline) and start a handle drag to confirm the dropzone outline is acce |
| SceneMenu (scene action bar) | `features/scenes/SceneMenu.tsx` | menu | ✓ | Convert the four expander buttons (Recall/Store/Clear/Cache) to the ToggleButton primitive with pressed={open===action} so the modern data-[state=on] accent applies automatically; drop the manual 'is-on' className. For the F-keys, either use ToggleButton press | Scroll to the Scene menu block under the table. In modern (dark+light): click Recall…/Store…/Clear…/Cache… and confirm the toggled button turns accent-filled (currently it stays flat). Store a scene on F3, expand Store…, and confirm F3 is visibly marked as hol |
| ViewList (named camera views) | `features/scenes/ViewList.tsx` | panel | ✓ | Make the row buttons ghost inside the pill in modern: cn('vwrow__name', modern && 'border-transparent bg-transparent rounded-none hover:bg-[var(--sh-btn-hover)]') and the same for .vwrow__del plus modern && 'border-l border-[var(--pm-line)]' to restore the div | In modern light+dark: type a name, press store, and confirm each view renders as a single seamless pill (name + × separated by one hairline divider, one outer border — not two nested borders). Hover name/× for the modern hover fill; click × to delete; enter an |
| Scene right-click menu (RowMenu) | `features/objects/RowMenu.tsx` | menu | — | Out of scope for the scenes files themselves — do not restyle it from features/scenes. Instead ensure the RowMenu/objects conversion adds a modern override (a shadcn.css .rowmenu block or component-level modern utilities, matching the .menu/.popup-menu frosted | Store scenes, then right-click a scene button in the overlay strip. In modern light+dark, confirm the popup gets the modern frosted panel / shadow / radius (like the menubar dropdowns) rather than the legacy dark box, that 'Scene <name>' header and the Rename/ |

## apps/web/src/features/wizards

_The wizards feature (PyMOL Wizard system: a launcher menu, a generic get_panel() renderer with button/popup/text rows, bespoke popup menus, a keyboard-capture strip, a pick/select seam, and a green-on-dark prompt overlay portalled over the viewport) — currently 100% un-converted: no component imports the ../../ui primitive layer, shadcn.css has zero .wiz* rules, and all styling is classic wizards.css in the legacy cascade layer, so the modern theme (especially light mode) breaks on dozens of hardcoded dark hex values (row buttons #43484d/#4a4f6b, inputs #14181c, hover #2b3a4a, errors #ff6b6b, etc.)."_

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| WizardsPanel | `features/wizards/WizardsPanel.tsx` | panel | — | 1) Add `import { useTheme } from '../../ui'` and `const modern = useTheme() === 'shadcn'`. 2) On the root div use cn('wizards', modern && 'font-[var(--pm-font-ui)] text-[12px] border-[var(--pm-line)]') so chrome becomes sans in modern while classic stays mono. | Open http://100.71.244.15:3005/?theme=shadcn (add &appearance=light for light mode). The wizard block sits at the bottom of the right-hand internal-GUI column, under the object list. With no wizard active only the 'Wizard' launcher bar shows. Confirm the block |
| WizardLauncher | `features/wizards/WizardLauncher.tsx` | toolbar | — | 1) Swap the three chrome buttons (toggle, dismiss, item, item--more, per-module) to the Button/IconButton primitive with variant='bare' (or 'menubar' for the ghost hover) so they inherit the GHOST Tailwind system; keep their BEM classes via className so tests/ | http://100.71.244.15:3005/?theme=shadcn — the 'Wizard' bar is always visible bottom-right. Click it to open the menu; expand 'All modules'. In light mode confirm: menu is a light floating card (not transparent), item hover is a light tint (not dark navy #2b3a4 |
| MenubarNode | `features/wizards/WizardLauncher.tsx` | menu | — | Covered by the WizardLauncher item conversion — swap to Button primitive variant='bare', gate the hover/disabled to modern tokens. Optionally replace the ▸/▾ text glyph with a lucide chevron in modern (rotate on aria-expanded). Keep inline paddingLeft indentat | Open the launcher menu (see WizardLauncher). If the catalog has submenu nodes they expand inline; confirm nested rows indent correctly and the shared hover/active states match the rest of the menu in both dark and light. |
| WizardPanel | `features/wizards/WizardPanel.tsx` | panel | — | 1) Gate error color to a token: on .wizpanel__error add `modern && 'text-[var(--sh-danger)] rounded-[var(--sh-r-sm)] bg-[var(--sh-accent-soft)]'` (or a soft danger tint) so it reads in both appearances. 2) .wizpanel__promptonly -> `modern && 'text-[var(--pm-te | Run `wizard measurement` (has rows) — the panel appears with a 'MEASUREMENT' header. Run `wizard demo` and drill into a sub-wizard to make depth>1 so the 'stack N' badge shows (hover it for the stack path). To see an error row, trigger a menu on a row that has |
| PanelRow | `features/wizards/WizardPanel.tsx` | control | — | Best: render type-2 and type-3 rows via the Button primitive (variant='bare' -> FLAT) and text rows via a plain span, gated to modern. Concretely: keep the wizrow BEM classes for classic, and add `modern && FLAT-equivalent`: for buttons `border border-[var(--s | Run `wizard demo` (button rows) and `wizard measurement` (popup rows + text). In light mode confirm: action buttons are light flat fills with dark legible labels (NOT dark grey blocks with invisible text), popup rows show a legible caret, text rows are readabl |
| WizardPopupMenu | `features/wizards/WizardPopupMenu.tsx` | overlay | — | Mirror the modern .menu treatment from shadcn.css §5 (this is the established pattern for bespoke menus). Either add `:root[data-ui-theme='shadcn'] .wizmenu{...}` rules, or gate Tailwind on the elements: shell `modern && 'bg-[var(--sh-panel-frost)] backdrop-bl | Run `wizard measurement`, then click a popup row (▾) in the panel to open this menu; hover a submenu item to open its flyout. In light mode confirm the popup is a light frosted card with a soft shadow, hover is the accent fill with white text (not dark navy),  |
| MenuItems | `features/wizards/WizardPopupMenu.tsx` | menu | — | Fully covered by the WizardPopupMenu conversion — the same gated Tailwind classes apply since these are the same BEM classes. No separate work beyond ensuring the flyout's border-left uses var(--pm-line) in modern for a softer seam (`modern && 'border-[var(--p | Same path as WizardPopupMenu; specifically hover a submenu row to open the nested flyout and confirm nested item styling and indentation match the parent menu in dark and light. |
| WizardPrompt | `features/wizards/WizardPrompt.tsx` | overlay | — | Minimal. KEEP the green text and mono font in both themes (it reads over the always-dark viewport — do not tokenize it to light values). Optional modern polish, gated to modern: soften the mode-1 backdrop from the flat rgb(51,51,51) to a rounded frosted chip,  | Run `wizard measurement` (sets a prompt like 'Select atoms...') — the green prompt appears at the top-left of the 3D viewport. Toggle appearance to light and confirm the prompt is UNCHANGED (green on dark) because it overlays the dark scene. Toggle a top seque |
| WizardKeyCapture | `features/wizards/WizardKeyCapture.tsx` | form | — | Swap the raw <input> for the TextInput primitive — it renders data-slot='input' and immediately picks up the modern field treatment from shadcn.css §6 (token bg var(--sh-btn-fill), border var(--sh-btn-border), radius, bright text) in both appearances, with the | Run `wizard pseudoatom` (implements do_key) — the 'this wizard is reading the keyboard' strip appears with the input auto-focused. Type characters and confirm the fake caret advances in the green viewport prompt (the value stays empty in this field by design). |
| WizardEventBridge | `features/wizards/WizardsPanel.tsx` | form | — | 1) Swap the <input> for TextInput (gets modern field styling, kills #14181c) keeping .wizevents__input. 2) Swap the three <button>s for the Button primitive variant='bare' (FLAT) keeping their DOM; they inherit the token'd fill/hover/disabled and lose the hard | Run `wizard measurement` (wants picks) — the 'pick seam' strip appears below the panel. Type a selection like `name CA`, confirm 'do_pick' enables; press Enter or the buttons. In light mode confirm the input is a light field and the three buttons are legible f |
| ColorCodedText | `features/wizards/ColorCodedText.tsx` | control | — | No structural conversion needed and none should be forced — the inline colors are semantic wizard output. Leave as-is. The surrounding surfaces (rows/menu/panel) provide the themeable background; just ensure those default (uncolored) spans sit on a token'd bac | Any wizard with colored text exercises it: `wizard appearance` (\RGB color swatches next to color names) or `wizard measurement`. Confirm the \RGB colors render identically in classic and modern, dark and light. Check that plain rows follow the theme text colo |
| wizard service/hooks (non-visual) | `features/wizards/service.ts` | service | — | None — no render output. Do not touch during the design-system conversion. | N/A — no visual surface. Their behavior is exercised indirectly by every wizard flow above (a wizard only appears because useWizard/service poll and populate the snapshot). |

## apps/web/src/features/mouse

_PyMOL mouse-mode UI: the ButMode readout block (mode/grid/selection lines) plus the 80-slot Mouse Configuration matrix; both are faithful ports styled by features/mouse/mouse.css in @layer legacy with hardcoded PyMOL text colors that fail in modern light mode._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ButModeBlock | `features/mouse/ButModeBlock.tsx` | panel | ✓ | 1) In ButModeBlock add `const modern = useTheme() === 'shadcn'` and import { useTheme, cn } from '../../ui'. 2) On each hand-written span/div apply modern-gated token utilities via cn(...) — utilities layer beats @layer legacy so they win without touching clas | Open front http://100.71.244.15:3005 with ?theme=shadcn&appearance=dark, then again with &appearance=light (or flip via the header ThemeToggle). The block sits at the bottom of the right internal-gui object column (visible whenever internal_gui is on). In LIGH |
| Mouse Mode cycle line | `features/mouse/ButModeBlock.tsx` | control | ✓ | Same as parent: gate token utilities on the child spans (label → text-[var(--pm-caption)], value → text-[var(--pm-mouse-mode)] with the light token added). Neutralize FLAT via a modern className on this Button (border-0 bg-transparent rounded-none w-full justi | In modern dark+light, hover shows a subtle token hover fill (not a hard classic gray), both words are legible, and the line height matches the grid rows. Click cycles the mode name; right-click opens the Mouse Config menu; confirm the echoed cmd.mouse('forward |
| ButMode action grid | `features/mouse/ButModeBlock.tsx` | control | — | Gate token color utilities on the spans (cn + modern): __rowlabel and __cell--head → text-[var(--pm-text-dim)]; __rowlabel--head → text-[var(--pm-mouse-mode)]; __cell → text-[var(--pm-text)]. Do NOT touch font-family (mono is intentional and already forced). O | Ensure mouse_grid is on (default for several modes). In LIGHT confirm each cell's 4-char action code is clearly readable and columns still line up; in DARK confirm colors read as muted tokens, not neon. Clicking anywhere on the grid still cycles the mode. |
| Selection level line | `features/mouse/ButModeBlock.tsx` | control | ✓ | Same span token utilities (label → --pm-caption, value → --pm-mouse-mode with light token added) and the FLAT neutralize className. Keep disabled + title logic. In the disabled/PkAt state consider modern && 'text-[var(--pm-text-dim)]' on the value to read as i | Toggle selection modes and confirm the level text is readable in both appearances; bind single-left to PkAt (or switch to a picking mode) and confirm the line goes non-interactive and its text still reads clearly in LIGHT. |
| ButMode warning banner | `features/mouse/ButModeBlock.tsx` | other | — | Introduce an --sh-warn token in both shadcn appearance blocks (e.g. #eab308 dark / #b45309 light) and apply modern && 'text-[var(--sh-warn)]' to the div; optionally modern && 'rounded-[var(--sh-r-sm)] bg-[color-mix(in_srgb,var(--sh-warn)_12%,transparent)] px-[ | Force the warning by putting the app in an unknown mouse mode (or simulate a read error) and confirm the banner is legible in LIGHT and DARK and matches the modern palette. |
| MouseConfigPanel | `features/mouse/MouseConfigPanel.tsx` | panel | ✓ | 1) Add modern gating (useTheme + cn from ../../ui). 2) Fix the header blue: className={cn(modern && 'text-[var(--pm-text-dim)]')} on the column <th> and the row-scope <th> (or add a shadcn.css rule `:root[data-ui-theme='shadcn'] .mousecfg__table th { color: va | Open front http://100.71.244.15:3005?theme=shadcn (dark then &appearance=light). The block is at the bottom of the right internal-gui column; click the 'Mouse & Keys…' button just under it to open the 'Mouse configuration' modal (data-testid=mouse-config). Con |
| Mouse-config action cell (Select) | `features/mouse/MouseConfigPanel.tsx` | control | ✓ | Mostly already done via the primitive. Optional polish only: add modern && 'text-[11px]' and a narrower width (~96px) via a modern-gated className on the Select to keep the 10-column matrix inside the modal without horizontal scroll; ensure the busy/disabled s | In the open modal (modern light + dark), open a cell dropdown: confirm the trigger has the modern field style, options are readable, focus shows the token ring, and '(unset)' appears only for unbound slots. Pick a new action and confirm the value persists and  |
| Mouse-config header + note | `features/mouse/MouseConfigPanel.tsx` | other | — | Give the <code> a modern chip: modern && 'rounded bg-[var(--sh-btn-fill)] px-1 font-[var(--pm-font-mono)] text-[var(--pm-text)]'. Optionally promote the title to the modern section-label language (modern && 'uppercase tracking-wide text-[10px] text-[var(--pm-t | In the open modal, confirm the title and note read clearly in LIGHT and DARK and the cmd.button token renders as a subtle chip rather than raw inline text. |

## apps/web/src/features/seqview

_The PyMOL "CSeq" sequence viewer — a virtualized, absolutely-positioned strip drawn over the 3D viewport that shows per-residue sequence rows with click/drag selection, a header status bar, a scrollbar that doubles as a selection minimap, and a right-click pymol.menu popup. The ONLY non-test .tsx is SequenceViewer.tsx (all other files are .ts logic: alignment/grammar/minimap/reserve/source/window/register); it also renders the shared RowMenu from features/objects. Nothing in the directory imports from ../../ui, and shadcn.css has NO .seqview* rules, so today the strip renders with its legacy-layer CSS in both themes (fine in modern-dark because --pm-* tokens resolve dark; broken in modern-light where the head/scroll strips flip to a light band over the always-dark rows)._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| SequenceViewer (root strip / CSeq block) | `features/seqview/SequenceViewer.tsx` | panel | — | Add `import { cn, useTheme } from '../../ui'` and `const modern = useTheme()==='shadcn'` at the top of SequenceViewer. Wrap the root className string in cn(...) and append modern-only classes. Keep the band DARK in both appearances (it floats over the always-d | Open front http://100.71.244.15:3005; load a structure then enable via the Display menu item 'Sequence' (seq_view) or the command line `set seq_view, 1`. Strip appears at the top of the viewport. Flip theme with the header ThemeToggle; force light with ?appear |
| Header status bar (.seqview__head) | `features/seqview/SequenceViewer.tsx` | toolbar | — | cn() the head className with modern-gated classes: pin a dark translucent fill so it does NOT flip in light mode, e.g. `bg-white/[0.04] text-[color:var(--pm-text-dim)] border-b border-[color:var(--pm-line)]`, and uppercase/tracked micro-labels to match shadcn  | With the strip visible, read the header on the dark rows in BOTH appearances (?appearance=dark / light) — it must stay dark/coherent, not a light band. Make a selection to populate the 'sele -> name' text; run `align`+`set seq_view` on two objects to show the  |
| Residue cell (.seqcell) — the core selection control | `features/seqview/SequenceViewer.tsx` | control | — | Leave geometry and inline colours entirely alone (they are load-bearing PyMOL metrics + data). Optional modern-only polish via cn on the cell: a `rounded-[2px]` and a hover `ring-1 ring-[var(--pm-line)]`, and a slightly softer selected outline — all gated to m | Click a residue (highlights + adds a minimap tick), shift/drag across several to extend a range, double-click to zoom-center, right-click to open the popup. Confirm cells stay a legible 8px-wide grid (not a 1px band) and selection tint reads in dark and light. |
| Row labels — breadcrumbs + residue numbers (.seqrow__labels) | `features/seqview/SequenceViewer.tsx` | other | — | Modern-gated cn on the crumb/num spans: crumbs `text-[color:var(--pm-accent)]` (or a dimmed accent), numbers `text-[color:var(--pm-text-dim)]`, optionally switch the tiny labels to the sans UI font. Low effort, purely token swaps. | Toggle label modes from the Display > Sequence submenu (seq_view_label_mode radios) or `set seq_view_label_mode, 2`; confirm breadcrumbs/numbers stay aligned over the cells and pick up the accent colour in modern. |
| Alignment fill run (.seqfill) | `features/seqview/SequenceViewer.tsx` | other | — | Essentially nothing required. Optional modern-gated `opacity-60` if the gap fill should read fainter in the modern look. Keep non-interactive. | Load two related structures, `align a, b`, `set seq_view` — the gap characters (default '-') appear between residues; confirm they stay behind (never intercept) cell clicks. |
| Row name label (.seqrow__name) | `features/seqview/SequenceViewer.tsx` | other | — | Modern-gated cn: `text-[color:var(--pm-text-dim)] font-sans` and a touch more right padding. Trivial. | `set seq_view_label_mode, 0`; each row shows a leading /name; confirm styling matches modern chrome. |
| Row truncation marker (.seqrow__more) | `features/seqview/SequenceViewer.tsx` | other | — | Optional modern-gated `text-[color:var(--pm-text-dim)]`. Negligible. | Load a large structure (e.g. many-residue chain, codes mode) so nCols exceeds the 1200-col WINDOW; the trailing … appears and the wheel/scrollbar reveal more. |
| Scrollbar + selection minimap (.seqview__scroll / .seqview__minimap / .seqview__tick / .seqview__thumb) | `features/seqview/SequenceViewer.tsx` | control | — | cn() the scroll className with modern-gated classes pinning a DARK track to match the header (`bg-white/[0.04] border-t border-[color:var(--pm-line)]`). Tick -> `bg-[color:var(--pm-accent)]`. Thumb -> a self-contained visible chip that works on any bg: `bg-[co | With a wide structure: click along the track to jump the window (thumb follows), wheel to scroll a column at a time, make selections and confirm the coloured minimap ticks appear at the right offsets. Critically switch to ?appearance=light and confirm the trac |
| Right-click context menu (RowMenu) — shared, rendered by seqview | `features/objects/RowMenu.tsx` | menu | — | Do not fork it inside seqview. Either (a) extend shadcn.css section 5 to also target .rowmenu/.rowmenu__panel/.rowmenu__row with the same frost bg + var(--sh-r) radius + accent-hover treatment the .menu rows get, or (b) migrate RowMenu onto the ../../ui Dropdo | Right-click a residue cell -> popup opens at the pointer; hover a submenu arrow to lazy-expand; pick a leaf and confirm the command echoes to the console and the view refreshes. Compare its look to a menubar dropdown in modern dark + light and note the inconsi |

## apps/web/src/features/settings

_The Settings overlay slot (registry region 'overlay', WP-15): a top-right launcher that opens one floating window hosting three surfaces — a data-driven menu renderer (Setting/Display/Mouse/Scene), the 779-row PyMOL Advanced Settings table, and the Lighting presets/sliders panel. Controls already route through the ui primitives, but every container/chrome class is bespoke BEM in settings.css (@layer legacy) that only reads --pm-* tokens plus several hardcoded dark-tuned literals, so it recolours but never modernizes and several literals break in light mode._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| SettingsPanel (root orchestrator) | `features/settings/SettingsPanel.tsx` | overlay | — | This file imports nothing from ../../ui. First add `import { useTheme } from '../../ui'` (or the primitives) and `const modern = useTheme() === 'shadcn'` so the launcher, window and tabs below can be gated. It doesn't paint anything itself — the work is in the | Open the app in modern theme (`?theme=shadcn&appearance=light` and again `&appearance=dark`, or use the ThemeToggle in the AppShell header). The launcher sits at the top-right corner of the viewport; clicking Setting/Edit All…/Lighting must open exactly one wi |
| Settings launcher (.setlaunch) | `features/settings/SettingsPanel.tsx` | toolbar | — | Replace the three raw <button>s with the primitive Button (variant 'launcher' or 'quick') so they inherit the modern FLAT/GHOST Tailwind treatment and focus ring; or gate Tailwind classes (`rounded-[var(--sh-r-sm)] border border-[var(--sh-btn-border)] bg-[var( | In modern light AND dark: the three launcher buttons and the '… settings' counter appear top-right. Force an error (disconnect the API) and confirm the counter/error text is legible on the light panel (currently the #ff8080 pastel is the thing to check). |
| Settings window shell (.setwin / .setwin__title / .setwin__body / .setwin__status) | `features/settings/SettingsPanel.tsx` | panel | — | This is a floating surface but does NOT use the Panel primitive, so the shadcn.css `[data-slot='panel']` frost/radius/shadow rule never touches it. Either wrap the window in `<Panel>` (gains frost + `--sh-pop` + `--sh-r` for free) or gate Tailwind: `modern &&  | Open any of the three windows in modern light + dark. Check: window has a soft frosted card look (not a flat black-shadowed box) in both appearances; the title strip text is legible; the '×' has a visible hover/focus target. Resize between menu (380px) and tab |
| Menu tabs (.setmenu__tabs) | `features/settings/SettingsPanel.tsx` | toolbar | — | Convert the four tabs to `<ToggleButton pressed={which===name}>` (variant 'menubar' or a segmented look) so the modern accent-fill on/off state (already defined for `[data-slot='toggle-button'][data-state='on']` in shadcn.css) applies — today `.setmenu__tab.is | Open the 'Setting' window (menu). In modern dark + light, click through Setting/Display/Mouse/Scene: the selected tab should read as clearly active (accent-tinted) and legible in both appearances; the 'get_menudata' tag stays muted and right-aligned. |
| MenuDataRenderer + MenuNodeView (the menu-data rows) | `features/settings/SettingMenu.tsx` | menu | ✓ | INCONSISTENCY TO FIX FIRST: the submenu row is the only row rendered via <Button> (variant 'bare'), so in modern it picks up the FLAT bordered-pill utilities and looks like a button, while its sibling command/check/radio rows are native full-width flat menu ro | Open the Setting window with a connected session. In modern dark + light: hover rows (should show the accent hover from the .menu__row convention), toggle a check item (e.g. under Display) and watch ✓ appear/`.is-on` state; expand a submenu (▸/▾) and confirm t |
| LightingPanel (presets + slider sections + note) | `features/settings/LightingPanel.tsx` | form | ✓ | Preset Buttons already modernize (bare -> FLAT). Slider + TextInput already restyle via shadcn.css data-slot rules, so they're fine in both appearances. Remaining work is the container chrome + two hardcoded legend/label colours: gate `modern` and replace `.li | Open the Lighting window with a session connected. Modern dark + light: the five preset buttons look like the app's other modern buttons; each slider fills with the accent and its number field matches the modern input treatment; the 'Presets:' label and every  |
| AdvancedSettingsTable (bar + head + windowed body + footer) | `features/settings/AdvancedSettingsTable.tsx` | panel | ✓ | Filter TextInput and the Selects/inputs (via ScopeSelector) already restyle via data-slot rules. The bespoke chrome needs modern gating: `.setadv__bar`/`.setadv__head`/`.setadv__foot` are token-based (var(--pm-line)) so they recolour, but the head is a 9px upp | Open 'Edit All…' with a session. Modern dark + light: header/bar/footer legible; type in Filter and watch the count update and rows re-window smoothly; scroll the 700+ rows (no overlap/stacking — the setadv__ prefix exists to avoid the advset__ collision). Swi |
| ScopeSelector (scope/object/selection/state controls) | `features/settings/AdvancedSettingsTable.tsx` | form | ✓ | Controls are all primitives (Select/TextInput) so their modern paint is already handled by data-slot rules — good. Only the label wrappers need attention: `.setadv__scope label` uses var(--pm-text-dim) (fine, recolours). Ensure the fixed widths (`.setadv__sel{ | In 'Edit All…', change Scope to 'object' (Object select appears), to 'atom' (Selection field appears), to 'object-state'/'atom-state' (State field appears). In modern light + dark confirm each control is a modern field, labels are legible, and nothing overflow |
| Row + ValueEditor (advanced settings table row) | `features/settings/AdvancedSettingsTable.tsx` | control | ✓ | Checkbox/TextInput/Button are primitives (already restyle in modern). Fixes: `.setadv__row` border-bottom is a HARDCODED `rgb(52 56 60/35%)` dark hairline — invisible/near-wrong on light; override to `border-[var(--pm-line)]` under modern. `.setadv__error` #ff | In 'Edit All…' pick scope 'global'. Modern dark + light: toggle a boolean row's Checkbox; edit a text/number row and press Enter (commit) / Escape (revert); click ⟲ on an overridden row (should be enabled only when non-default) and confirm it fits the row with |
| ReinitializeStrip (File ▸ Reinitialize) | `features/settings/AdvancedSettingsTable.tsx` | toolbar | ✓ | Buttons are primitives (bare -> FLAT, fine in modern). The armed/destructive state is the problem: `.setadv__reinit button.is-armed{border-color:#b04a4a; color:#ffb0b0}` — the #ffb0b0 light-pink TEXT is essentially INVISIBLE on a white/light panel (the cleares | In 'Edit All…' scroll to the Reinitialize strip. Modern light + dark: click a destructive entry once (it must switch to the armed 'X?' state) and confirm the armed button is a strong danger red with readable text in BOTH appearances (the current #ffb0b0-on-lig |
| OverridesView + AtomStateStrip (per-selection status strips) | `features/settings/AdvancedSettingsTable.tsx` | other | — | Plain divs, no primitives, no interactivity — light work. `.setadv__overrides` uses var(--pm-text-dim) + border-top var(--pm-line) (token-based, recolours fine). Only `.setadv__hint` #6f767c is hardcoded -> `text-[var(--pm-text-dim)]` under modern. Optionally  | In 'Edit All…' set Scope to 'atom' or 'bond' and type a selection (e.g. 'elem C'): the strip should print the override readout with a legible muted hint caveat on the light panel. Set Scope to 'atom-state' and write a value to see the AtomStateStrip echo. |

## apps/web/src/features/files

_Parity area 6 — PyMOL's File menu: a "File ▾" overlay strip plus ~15 bespoke modal dialogs (open/save pickers, format-import forms, image/movie export, fetch, log, recent) and a server-side path browser. None of it touches the primitive layer yet; everything is hand-rolled raw HTML styled by BEM classes in features/files/files.css (now in @layer legacy). Most colors are --pm-* tokens that auto-adapt to modern light/dark, but a handful of hardcoded dark hexes (green/red banner blocks, pale-blue links, steel-blue dir names, mid-grey disabled) break in light mode, and all geometry (2px radius, sharp borders, mono inputs, hard black shadows) stays classic-looking in the modern theme._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| FilesPanel (File ▾ strip + dropdown menu + dialog orchestrator) | `features/files/FilesPanel.tsx` | menu | — | const modern = useTheme()==='shadcn'. Swap the trigger + Leave-presentation <button> for <Button variant='menubar'> (gains the modern ghost Tailwind automatically). For the dropdown, either (a) add [data-ui-theme='shadcn'] .files__menu/.files__menuitem/.files_ | Front http://100.71.244.15:3005. Set modern with the header ThemeToggle or ?theme=shadcn; switch dark/light with the appearance toggle or ?appearance=light. Click the 'File dialogs' pill in the floating overlay-launcher (bottom pill toolbar) to mount the panel |
| Modal / Field / Check / Preview / Unavailable (shared dialog chrome) | `features/files/Modal.tsx` | dialog | — | This is the one to convert first. Keep the DOM (tests pin it; the ui/Dialog primitive is explicitly NOT a drop-in). Add modern = useTheme()==='shadcn' and gate Tailwind: .fdlg → rounded-[var(--sh-r)] border-[var(--sh-line-strong)] bg-[var(--sh-panel-frost)] ba | Reach any dialog via File ▾ (e.g. files-menu-render). In modern light and dark confirm the card is a frosted rounded panel with a soft shadow, the title bar is a tracked uppercase label, and the red 'Not available in this build' block (open Load Maestro/MTZ) i |
| PathPicker (server-side file browser) | `features/files/PathPicker.tsx` | dialog | — | Gate modern Tailwind: replace .fpick__crumb/.fpick__toggle/.fpick__place with <Button variant='bare'/'menubar'>; footer buttons → <Button> (secondary) + primary accent Button for accept; filter <select> → <Select>, name field → <TextInput>, goto input → <TextI | File ▾ ▸ Open… → picker opens. In modern light/dark confirm: directory names are readable (not pale steel-blue), selected row uses accent, buttons/select/inputs match the modern field style, footer error uses the danger token. Also reach via File ▾ ▸ Working D |
| TrajDialog (Load Trajectory) | `features/files/LoadDialogs.tsx` | form | — | After Modal is converted, swap raw <select>→<Select>, <input type=number>→<TextInput> (or a Number wrapper), keep Check→<Checkbox>. Gate .fdlg__group to modern (rounded var(--sh-r-sm), border var(--pm-line), legend → tracked uppercase var(--pm-text-dim)) and . | Requires a trajectory file on the server (e.g. a .dcd/.xtc alongside a loaded object). File ▾ ▸ Open… a trajectory → this modal appears. Easier: drive via existing dom test flow. In modern light/dark check fieldset borders/legends and mono number fields read a |
| MapDialog (Load Map) | `features/files/LoadDialogs.tsx` | form | — | As TrajDialog: <input>→<TextInput>, <select> n/a, Checks→<Checkbox>; gate fieldsets + .fdlg__row spacing to modern. Keep datalists. Primary Load → accent Button. | Needs a map file (.ccp4/.map/.xplor) opened via File ▾ ▸ Open…. Confirm the wide modal, both fieldsets and the checkbox+input rows render in the modern field style in light and dark; Preview box tokenized. |
| MaeDialog (Load Maestro File) | `features/files/LoadDialogs.tsx` | form | — | Fix comes mostly from converting <Unavailable> in Modal.tsx (token danger tint). Locally: <select>→<Select>, inputs→<TextInput>, mimic Check→<Checkbox>; Load→primary accent Button whose disabled state uses opacity not #5b6167. | Open a .mae/.maegz via File ▾ ▸ Open…. In this build it is 'unavailable', so the red block always shows — perfect to verify light-mode: it must become a soft danger tint, not a dark maroon box, and disabled Load must look disabled in both appearances. |
| MtzDialog (Load Reflections) | `features/files/LoadDialogs.tsx` | form | — | Convert via Modal (unavailable) + swap <select>→<Select>, inputs→<TextInput>; change the local .fdlg__error usage to var(--sh-danger). Load→accent Button. | Open an .mtz via File ▾ ▸ Open…. Check the danger block + any error line use the danger token in light and dark; selects/inputs modern. |
| MapGenerateDialog (PyMOL Map Generation) | `features/files/LoadDialogs.tsx` | form | — | Same recipe: Modal Unavailable token fix, <select>→<Select>, inputs→<TextInput>, Check→<Checkbox>, OK→accent Button with opacity-based disabled; local error→var(--sh-danger). | File ▾ ▸ Generate Map from Reflections… → pick any .mtz → this modal (with the always-on build-note block, ideal for light-mode banner check). Confirm token danger tint + modern fields in both appearances. |
| AlnDialog (Load Alignment) | `features/files/LoadDialogs.tsx` | form | — | Swap <select>→<Select>; footer → secondary + accent Buttons. Style inline <code> chips with a modern token bg if desired. Otherwise inherits Modal conversion. | Open a .aln/.fasta containing alignments via File ▾ ▸ Open…. Confirm the per-record selects and both footer buttons render modern in light/dark. |
| PartialDialog (Load Session — merge/discard) | `features/files/LoadDialogs.tsx` | form | — | Gate native radios to accent-color:var(--pm-accent) under modern (mirror the [data-slot] checkbox rule; add a data-slot='radio' primitive or a shadcn.css [data-ui-theme='shadcn'] .fdlg__radio input rule). Change .is-disabled color to var(--pm-text-dim). Check→ | Open a .pse while a session is already loaded (needs the partial gate) → this modal. Confirm radios show accent when selected, the disabled 'Open in new PyMOL Window' row is clearly dimmed in light and dark. |
| PngDialog (Save PNG image) | `features/files/ImageDialogs.tsx` | form | — | Convert .fdlg__banner (shared) to a token info style — e.g. bg-[var(--sh-accent-soft)] text-[var(--pm-text)] or a defined success token — instead of #0c3a0c. Locally: <select>→<Select>, footer buttons → secondary + accent Button. | File ▾ ▸ Export Image As ▸ PNG… (files-menu-png). In light mode confirm the info banner is a soft token tint, not a dark-green box; the select + buttons are modern; repeat in dark. |
| RenderPanel (Draw / Ray) | `features/files/ImageDialogs.tsx` | form | — | Inputs→<TextInput>, Units→<Select>, Checks→<Checkbox>. Footer + Reset → <Button> (Ray/Save primary accent, others secondary). Inherits Modal card conversion. | File ▾ ▸ Draw / Ray… (files-menu-render) — reachable with no file loaded. Verify page 1 fields + the two-button footer, click Draw to reach page 2 and check its 3-button footer + code text, in light and dark. |
| MovieDialog (Export Movie) | `features/files/ImageDialogs.tsx` | form | — | Fix banner + unavailable via the shared Modal token conversion. Gate radios to accent-color. <select>→<Select>, number inputs→<TextInput>. Resolution shortcut pills + Save → <Button> (Save primary accent). Fieldset borders/legends → modern token treatment. | File ▾ ▸ Export Movie As ▸ MPEG… (files-menu-movie-mpeg). In light/dark confirm: no-frames banner is a soft tint, an uninstalled-encoder line is a token danger tint (pick 'convert' if not installed), radios show accent, the 720p pills and Save button are moder |
| ExportMoleculeDialog (Export Molecule, tabbed) | `features/files/SaveDialogs.tsx` | form | — | Convert the tab strip to a modern segmented control: gate .fdlg__tabs to a rounded pill group, active tab → bg var(--sh-accent-soft)/text var(--pm-text-bright) or a ToggleButton pattern; or build with ToggleButton primitives. Checks→<Checkbox>, radios→accent-c | File ▾ ▸ Export Molecule… (files-menu-export-molecule). Click through Options/PDB/Multiple files tabs; in light/dark confirm the active tab reads as a modern selected segment (not a classic notched tab), checks/radios show accent, footer filter+Save are modern |
| SaveObjectDialog (Export Map / Export Alignment) | `features/files/SaveDialogs.tsx` | form | — | <select>→<Select>; footer → secondary + accent Buttons. Style the empty-state <pre> with modern token text/spacing. Inherits Modal conversion. | File ▾ ▸ Export Alignment… (files-menu-export-aln) with nothing loaded → the Warning/empty variant (good to check <pre> text in light mode). File ▾ ▸ Export Map… with a map loaded → the select variant. |
| FetchDialog (Get PDB) | `features/files/ToolsDialogs.tsx` | form | — | Convert .fdlg__banner to a token info tint; change .fdlg__link to text-[var(--pm-accent)] (readable in both). Inputs→<TextInput>, Checks→<Checkbox>. Download→accent Button with opacity-based disabled. Fieldset → modern token treatment. | File ▾ ▸ Get PDB… (files-menu-fetch). In LIGHT mode specifically confirm the 'change…' link is visible (accent), the banner is a soft tint not dark-green; type a 4-char code to enable Download and check its modern primary style; repeat dark. |
| LogDialog (Log File hub) | `features/files/ToolsDialogs.tsx` | form | — | All action buttons → <Button variant='bare'>; footer Close → secondary Button. Disabled state via opacity not #5b6167. Inherits Modal conversion. | File ▾ ▸ Log File ▸ … (files-menu-log). Confirm the four-button row + Close render modern; with no log open the Close-log button is clearly disabled in light and dark. |
| RecentDialog (Open Recent) | `features/files/ToolsDialogs.tsx` | dialog | — | Gate rows to modern (rounded var(--sh-r-sm), hover accent already token); change .is-missing to text-[var(--pm-text-dim)] with maybe a line-through. Close → secondary Button. | File ▾ ▸ Open Recent… (files-menu-recent). Confirm rows hover with accent and any missing entry is a token-dimmed style (not brown) in light and dark. |
| FileDropTarget (window drag-drop + Ctrl+O/S + menu-hook host) | `features/files/FileDropTarget.tsx` | service | — | Optional only: add a modern drag-over overlay (a themed 'Drop files to load' scrim) for feedback — a new element gated to modern with Tailwind. The native OS picker cannot be themed. No required light-mode fix. | Always mounted. Drag a .pdb over the window → copy cursor + load. Press Ctrl+O → native file picker (not app-themed). No dialog chrome to check; verify it does not regress dialogs it hosts. |
| PluginDialogHost (blocked-plugin file dialog host) | `features/files/PluginDialogHost.tsx` | service | — | None locally beyond keeping the './files.css' import; fixing PathPicker fixes this. If PathPicker is split into primitives, ensure this host still mounts the same modern styles (it is always-mounted, so the modern CSS/utilities must not depend on FilesPanel be | Hard to reach without a legacy plugin calling tkinter.filedialog.askopenfilename(). Verify indirectly via PathPicker (identical DOM). If a plugin is available, trigger its file dialog and confirm the picker appears themed in modern light/dark even with the Fil |
| OpenProgress (multi-file open queue readout) — defined, not currently mounted | `features/files/LoadDialogs.tsx` | control | — | If wired up, gate chips to modern (rounded token pills, is-now → accent-soft). Low priority since unmounted; decide whether to mount it in place of the .files__busy string. | Not reachable in the UI as-is (only exercised by dom tests). To verify, temporarily render it from FilesPanel during a multi-file Open, or check its dom test. |

## apps/web/src/features/dialogs

_Area-10 floating dialog system — a draggable/resizable window frame, a bottom-left launcher strip, the Advanced Settings table, and three small modals (text/number/confirm) shared by the volume + text-editor features; all chrome is hardcoded PyMOL-light CSS in @layer legacy that is NOT theme-gated, so it currently paints the modern theme too._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| DialogWindow | `features/dialogs/DialogWindow.tsx` | panel | ✓ | This component does not call useTheme. Two routes: (A cleanest) add data-slot="panel" to the .dlgwin root div so shadcn.css section 8 gives it the frost card free (background var(--sh-panel-frost), backdrop-filter var(--sh-blur), border var(--sh-line-strong),  | Open http://100.71.244.15:3005/?theme=shadcn&appearance=dark. The launcher strip is fixed bottom-left; click 'Advanced settings' (data-open) to spawn a DialogWindow. Check the window is a dark frosted card (not a light-gray box), text is legible on dark, the t |
| AdvancedSettings | `features/dialogs/AdvancedSettings.tsx` | form | ✓ | Add modern=useTheme()==='shadcn' and gate Tailwind onto the containers: .advset__scroll -> bg-[var(--pm-panel-alt)] border border-[var(--pm-line)] rounded-[var(--sh-r-sm)]; .advset__row -> border-b border-[var(--pm-line)]; .advset__error -> text-[var(--sh-dang | At /?theme=shadcn&appearance=dark, click 'Advanced settings' in the launcher. Confirm the scroll body is a dark panel with faint token hairlines (not white), the filter field matches other modern inputs (single ring/border), rows show mono names with legible v |
| Launcher | `features/dialogs/DialogsPanel.tsx` | toolbar | ✓ | Gate modern Tailwind on the .dlglaunch container to mirror the overlay-launcher pill treatment already in shadcn.css section 11: bg-[var(--sh-panel-frost)] backdrop-blur border border-[var(--sh-line-strong)] rounded-full px-1.5 py-1 gap-1 shadow-[var(--sh-pop) | At /?theme=shadcn&appearance=light the strip is visible bottom-left immediately — it must read as a light frosted pill with dark text and token buttons, NOT a dark charcoal bar. At appearance=dark it should be a dark frosted pill (not flat #1c1c1c). Click each |
| DialogsPanel | `features/dialogs/DialogsPanel.tsx` | service | ✓ | No direct styling work. Confirm no wrapper element is added that would break the portal/positioning of its children. All modern work is in DialogWindow (shared chrome), AdvancedSettings, Launcher, and the three modals; the volume/properties/texteditor panels a | At /?theme=shadcn, confirm all four launch paths still mount (click Properties, Advanced settings, Text editor; pick a volume) and that multiple windows can be open at once and raised independently — i.e. the host still dispatches correctly after any chrome ch |
| TextDialog | `features/dialogs/modals.tsx` | dialog | ✓ | Share one modern treatment across all three modals via `modern &&` gating: .dlgmodal__box -> bg-[var(--sh-panel-frost)] text-[var(--pm-text)] border border-[var(--sh-line-strong)] rounded-[var(--sh-r)] shadow-[var(--sh-pop)] backdrop-blur; .dlgmodal__pre -> bg | TextDialog is opened from the volume editor, which needs a loaded object:volume: at /?theme=shadcn&appearance=dark, load a map+create a volume, open its panel from the launcher 'volume panel…' select, then click 'Get colors as script' (or Help). Confirm the mo |
| NumberPrompt | `features/dialogs/modals.tsx` | dialog | ✓ | Reuse the shared modal-box modern classes from TextDialog (bg --sh-panel-frost, text --pm-text, border --sh-line-strong, rounded --sh-r, shadow --sh-pop, backdrop-blur) on .dlgmodal__box--narrow. For .dlgmodal__input, drop the redundant legacy border/radius/mo | Open a volume panel (see TextDialog) and double-click a ramp stop value / edit vmin/vmax/amax to raise NumberPrompt. At /?theme=shadcn&appearance=dark confirm the 280px box is a dark frosted card, the number field matches other modern inputs (single ring), the |
| ConfirmPrompt | `features/dialogs/modals.tsx` | dialog | ✓ | Reuse the shared modal-box modern classes (as TextDialog/NumberPrompt) on .dlgmodal__box--narrow and .dlgmodal__title. .dlgmodal__message -> text-[var(--pm-text)]. Optionally elevate the primary action: give the confirming Button an accent treatment (bg-[var(- | At /?theme=shadcn&appearance=dark open the Text editor from the launcher, type to make the buffer dirty, then trigger the save guard (close/switch buffer with unsaved changes) to raise ConfirmPrompt. Confirm the box is a dark frosted card with a readable messa |

## features/builder

_The molecular Builder — a single floating overlay dock (BuilderPanel.tsx + builder.css) porting PyMOL's QDockWidget: tabbed fragment/residue/base palettes plus always-visible atom/bond/model action rows, a pick strip, a wizard strip, confirm modals and a footer. Leaf Buttons/Select/Checkbox already route through the src/ui primitives and inherit modern styling; the container chrome is styled directly by builder.css using the never-defined vars --panel/--panel-2/--line (always falling back to hardcoded darks) plus several hardcoded dark hex, so every framing surface stays near-black and unreadable in modern LIGHT appearance._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| BuilderPanel (floating dock shell) | `features/builder/BuilderPanel.tsx` | panel | — | Root .builder div is a raw div, not a primitive. Add `const modern = useTheme() === 'shadcn'` (useTheme is re-exported from ../../ui) at the top of BuilderPanel and wrap the className in cn('builder', modern && '...'). Gate these Tailwind utilities on modern s | Front at http://100.71.244.15:3005. Load with ?theme=shadcn&appearance=light (or use the shell-header ThemeToggle to pick Modern + Light). Open the dock via the floating 'Builder' button top-left, or the console Quick Buttons 'Builder' (Blocks icon). Check the |
| Builder launch button (closed state) | `features/builder/BuilderPanel.tsx` | control | ✓ | Already a Button primitive (variant 'bare'), so in modern it receives the FLAT utility set (bg/border/text from --sh-btn-* tokens) which overrides the legacy .builder-launch fill/border and is light-correct. Minimal work: only its position/size come from .buil | In modern light with no dock open, confirm the top-left 'Builder' button is a light flat button (not a dark chip). Click it to open the dock. |
| Title bar (busy dot, mouse-mode readout, close) | `features/builder/BuilderPanel.tsx` | toolbar | ✓ | 1) Add modern-gated `border-b border-[var(--pm-line)]` to .builder__title (currently border-bottom:var(--line) dark). 2) Busy dot #d8a13a: swap to modern `bg-[var(--pm-accent)]` (or a warning token) via modern &&. 3) The close IconButton uses variant 'bare' so | Open the dock in modern light: the header underline should be a subtle light divider, the close control a borderless × that highlights on hover (today it is a bordered box), and the busy dot readable. |
| Error / drift banner | `features/builder/BuilderPanel.tsx` | other | — | Raw div. Add modern-gated Tailwind to .builder__error: `border border-[color-mix(in_srgb,var(--sh-danger)_45%,transparent)] bg-[color-mix(in_srgb,var(--sh-danger)_12%,transparent)] text-[var(--sh-danger)] rounded-[var(--sh-r-sm)] px-2 py-1`. --sh-danger is def | Trigger an error (e.g. arm a bond wizard then click a single-atom pick, or cause any act() failure) in modern light and confirm the banner is a soft red-tinted strip with readable red text, not a black maroon block. The drift banner only shows if the button ta |
| PickStrip (pk1–pk4 chips, bond, frag, unpick) | `features/builder/BuilderPanel.tsx` | toolbar | ✓ | unpick button already FLAT-styled in modern (fine). Chips are raw spans: add modern-gated classes to .builder__pick `border border-dashed border-[var(--pm-line)] rounded-[var(--sh-r-sm)] px-1.5`, to .is-set `border-solid border-[var(--pm-accent)] text-[var(--p | Modern light, dock open: the four pk chips should show a light dashed outline; load a small molecule, enter editing mode and pick atoms in the viewport so chips flip to .is-set — confirm the filled/bond states use accent-colored solid borders that read clearly |
| Tab bar (Chemical/Protein/Nucleic + nested DNA/RNA) | `features/builder/BuilderPanel.tsx` | toolbar | ✓ | Tabs are Button primitives, so modern adds FLAT to EVERY tab — which (a) makes them bordered pills instead of a tab strip and (b) overrides the .is-active legacy background so the ACTIVE tab looks identical to inactive (active indicator disappears in modern).  | Modern light: the tab row should read as tabs with a clear active indicator (underline/accent). Click through Chemical / Protein / Nucleic Acid and DNA/RNA — confirm the selected tab is visually distinct (today it is not in modern). |
| Element buttons (Chemical tab row 0) | `features/builder/BuilderPanel.tsx` | control | ✓ | Already Button primitives (variant bare) -> modern FLAT applies (bg/border/text from --sh-btn tokens, rounded-6, h-7 px-3), which is light-correct. Only concern is DENSITY: h-7 (28px) + px-3 is chunky for this tight PyMOL-parity grid. Optional: introduce a com | Modern light, Chemical tab: element buttons should be light flat buttons with a subtle border and hover; confirm they are NOT dark chips. Sizing/legibility check only. |
| GrowButton (fragments + functional groups) | `features/builder/BuilderPanel.tsx` | control | ✓ | Same as element buttons — already FLAT in modern and light-correct. No change required beyond the optional shared compact-density tweak for all .bbtn. | Modern light, Chemical tab rows 1-2 (fragments, functional groups): confirm consistent light flat buttons matching the element row. |
| Ring buttons + RingGlyph icon (filter bug) | `features/builder/BuilderPanel.tsx` | control | ✓ | The IconButton is FLAT-correct. THE BUG is the icon inversion: .bbtn__icon uses filter:invert(1) with an @media (prefers-color-scheme: light){filter:none} override — keyed to the OS color scheme, NOT the app's data-appearance. In modern LIGHT on a dark-OS the  | Modern light, Chemical tab row 3 (ring icons): the 10 ring line-drawings must be dark on a light button. Toggle appearance dark<->light via the ThemeToggle and confirm the glyphs invert with the theme, not with the OS setting. Force a mismatch with ?appearance |
| Amino-acid buttons (Protein tab) | `features/builder/BuilderPanel.tsx` | control | ✓ | Already FLAT in modern, light-correct. Optional shared density tweak only. | Modern light, Protein tab: two rows of light flat residue buttons. |
| Secondary-structure Select + note (Protein tab) | `features/builder/BuilderPanel.tsx` | form | ✓ | Select is the Form primitive (data-slot='select'); shadcn.css:283-294 already styles it (--sh-btn-fill/--sh-btn-border/--pm-text-bright, radius, padding) and it flips in light — already theme-correct. Only the raw <label> and .builder__note are un-tokenized te | Modern light, Protein tab: the Secondary Structure dropdown should be a modern light field; change it and confirm the phi/psi note updates and text is readable. |
| Action rows: ActionButton + section labels | `features/builder/BuilderPanel.tsx` | control | ✓ | ActionButtons are Button primitives -> FLAT-correct in modern. .builder__label spans are raw text (opacity 0.75, inherit) and are fine in light; optionally tokenize to `text-[var(--pm-text-dim)] uppercase tracking-wide text-[10px]` under modern to match the sh | Modern light: the Atoms/Bonds/Model rows should be light flat buttons with readable section labels; click Sculpt/Fix/Rest to confirm they still fire (footer sculpt readout updates when sculpting). |
| Clean (disabled) button | `features/builder/BuilderPanel.tsx` | control | ✓ | Modern FLAT already applies `disabled:opacity-45 disabled:pointer-events-none`; the legacy .bbtn--disabled line-through survives (utilities set no text-decoration) so the struck-through disabled look is preserved in both themes. No light-mode fix needed; optio | Modern light, Bonds row: 'Clean' should appear as a struck-through, dimmed, non-interactive light button with the incentive-only tooltip. |
| Settings checkboxes + Undo/Redo row | `features/builder/BuilderPanel.tsx` | form | ✓ | Checkbox is the Form primitive (data-slot='checkbox'); shadcn.css:296-299 sets accent-color:--pm-accent — theme-correct. Undo/Redo are FLAT Buttons — correct. The wrapping .builder__check label text inherits (fine). Essentially covered; only optional density. | Modern light, third action row: checkboxes should show the indigo accent when checked; Undo/Redo are light flat buttons; test Ctrl-Z / Ctrl-Shift-Z inside the dock. |
| DnaTab (bases + native radios) | `features/builder/BuilderPanel.tsx` | form | — | The base buttons are FLAT-correct. The radios BYPASS the primitive layer (plain <input type='radio'>, no data-slot) so they get default browser rendering and, unlike the Checkbox primitive, do NOT pick up accent-color. Fix: either route them through a small pr | Modern light, Nucleic Acid -> DNA subtab: base buttons light+flat; confirm the Form (A/B) and Helix (Single/Double) radios render as light-mode radios and match the accent used by the settings checkboxes. |
| RNA bases + hint links | `features/builder/BuilderPanel.tsx` | other | ✓ | Base buttons FLAT-correct. The <a> hint links use default anchor styling; add modern-gated `text-[var(--pm-accent)] underline underline-offset-2` so links read as accent in both appearances. .builder__hint/.builder__note are opacity-dimmed inherited text (fine | Modern light, Nucleic Acid -> RNA subtab: base buttons light+flat; confirm the 'Hint: also check out …' links are accent-colored and legible, and the form-A caption text is readable. |
| WizardStrip (armed-wizard callout) | `features/builder/BuilderPanel.tsx` | overlay | ✓ | Inner buttons are FLAT-correct, but the CONTAINER is a raw div with a hardcoded dark navy fill that breaks light mode. Add modern-gated Tailwind to .builder__wizard: `border border-[var(--pm-accent)] bg-[var(--sh-accent-soft)] text-[var(--pm-text)] rounded-[va | Modern light: arm a wizard by clicking a palette button with nothing picked (e.g. an element button arms ReplaceWizard, or Delete arms RemoveWizard). The wizard strip must render as a light, accent-tinted callout with readable prompt text and a working 'dismis |
| ConfirmModal (Clear / Enable-undo dialogs) | `features/builder/BuilderPanel.tsx` | dialog | ✓ | Best path: migrate to the Radix `Dialog` primitive (Dialog/DialogContent/DialogTitle from ../../ui) which shadcn.css already styles (frosted panel, --sh-pop, focus trap, Escape) and is theme-correct — the current hand-rolled modal has no focus trap or Escape.  | Modern light: click 'Clear' in the Atoms row to open the confirm dialog — the box must be a light card with readable text and a dimmed backdrop (today it is a black box). Also reachable via the Enable-Undo prompt when toggling the undo setting on objects that  |
| Footer (picked / sculpt / object counts) | `features/builder/BuilderPanel.tsx` | other | — | Raw div of dimmed text; already legible in light (inherits text token at opacity 0.6). Optional polish: modern-gated `border-t border-[var(--pm-line)] pt-1 text-[var(--pm-text-dim)]` to visually separate it and match the modern muted-caption treatment. | Modern light: footer text (picked / sculpting strain / N object(s)) should be a subtle muted caption; start Sculpt to see the strain/cycles readout update. |

## features/colors

_The colours feature — a single floating overlay window ("C" launcher) with six tabs (palette/bands/editor/spectrum/ramps/space) reproducing PyMOL's whole colour system; ColorsPanel/ColorEditor/SpacePanel already use ui primitives, but SwatchGrid/BandGrid/SpectrumPanel/RampPanel are still raw button/input/select styled entirely by colors.css (all in @layer legacy)._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ColorsLauncher (collapsed 'C' button) | `features/colors/ColorsPanel.tsx` | control | ✓ | Add const modern = useTheme()==='shadcn'. Gate Tailwind onto the launch button via cn('colors-launch', modern && 'rounded-full border-[var(--sh-btn-border)] bg-[var(--sh-panel-frost)] px-3 py-1.5 backdrop-blur hover:bg-[var(--sh-btn-hover)] shadow-[var(--sh-po | Open http://100.71.244.15:3005?theme=shadcn (then repeat with &appearance=light). The floating 'C' pill is fixed at bottom-right (right:12px, bottom:200px). Check the pill frost/shadow renders and the rainbow strip is visible in both dark and light; click it t |
| ColorsWindow (dialog frame: titlebar, status, refetch, close, warn banner, selection field, tab strip) | `features/colors/ColorsPanel.tsx` | dialog | ✓ | Add modern gate. Convert the window frame to a Panel-style card: on .colors add modern && 'rounded-[var(--sh-r)] border-[var(--sh-line-strong)] bg-[var(--sh-panel-frost)] backdrop-blur shadow-[var(--sh-pop)]'. Recolor .colors__warn with tokens instead of hex,  | Open the window, toggle appearance light/dark. Confirm the frame reads as a frosted card, the tab strip has a clear active state, and — critically — the selection field and (after wiring) all child fields are LIGHT (not near-black) in light mode. Force the war |
| SwatchGrid (palette tab — the PyMOL 'C' menu: submenu tab strip + 9 swatch groups + live ramps) | `features/colors/SwatchGrid.tsx` | menu | — | Add modern gate. Convert the 7 submenu tabs to ToggleButton(variant='quick', pressed=open===id) so they get the FLAT look + accent 'on' fill. Recolor the shared .cmenu__sub wrapper: modern && 'rounded-[var(--sh-r-sm)] border border-[var(--sh-line-strong)] bg-[ | In the open window pick the 'palette' tab. In modern light mode, confirm the 9 swatch groups render on a light card (the .cmenu__sub sub-panels are not dark boxes), the submenu tab strip has a modern active state, and clicking a swatch recolours the scene. Tri |
| ByElement submenu (by element / non-carbon / fixed-carbon shortcuts) | `features/colors/SwatchGrid.tsx` | menu | — | Covered by the shared .cmenu__sub / .cmenu__note / .cmenu__page fixes on SwatchGrid. Additionally: page tabs → ToggleButton(pressed) for accent state; cnc/shortcut rows → keep as .cmenu__row but add modern && rounded-[var(--sh-r-sm)] hover treatment; leave til | Palette tab → click 'by element'. Toggle light mode: confirm the page tabs (organic/inorganic/…) show a modern active pill, the note is legible, the sub-panel is a light card, and element/shortcut tiles keep their true colours. |
| ByChain submenu | `features/colors/SwatchGrid.tsx` | menu | — | Covered by shared SwatchGrid .cmenu__sub / .cmenu__row modern treatment; give rows modern && 'rounded-[var(--sh-r-sm)]' hover. Cycle strip needs no data change. | Palette tab → 'by chain'. In light mode confirm rows are legible on a light card, hover shows the accent fill, and the colour-cycle strip renders its true colours. |
| BySs submenu (by secondary structure) | `features/colors/SwatchGrid.tsx` | menu | — | Covered by shared .cmenu__sub / .cmenu__row modern treatment; no local colour fix needed beyond the wrapper. | Palette tab → 'by ss'. Light mode: rows legible on a light card, the Helix/Sheet/Loop chips show their colours, hover fills accent. |
| ByRep submenu (per-rep colour settings) | `features/colors/SwatchGrid.tsx` | menu | — | Covered by shared fixes; additionally make the arm row a ToggleButton(pressed=active===setting) for a clear accent 'armed' state, mode tabs → ToggleButton(pressed), and .cmenu__unset → Button(variant='bare') small. Fix .cmenu__note grey via the shared token sw | Palette tab → 'by rep'. Light mode: mode tabs + note legible on a light card. Arm a setting (row highlights), confirm the SwatchGrid armed banner appears, click 'unset', and confirm the armed state reads clearly. |
| SpectrumShortcuts submenu | `features/colors/SwatchGrid.tsx` | menu | — | Covered entirely by the shared SwatchGrid .cmenu__sub + .cmenu__row modern treatment; no local colour fix. | Palette tab → 'spectrum'. Light mode: rows legible on a light card, accent hover, clicking runs a spectrum on the current selection. |
| AutoColor submenu | `features/colors/SwatchGrid.tsx` | menu | — | Covered by the shared SwatchGrid treatment; no local colour fix. | Palette tab → 'auto'. Light mode: rows legible on a light card, accent hover, clicking recolours. |
| Negative submenu (mesh/surface negative colour) | `features/colors/SwatchGrid.tsx` | menu | — | Covered by shared fixes; make the arm row a ToggleButton(pressed=armed) and page tabs ToggleButton(pressed) for clear state. Shared token swap fixes the note. | Palette tab → 'negative'. Light mode: rep tabs + note legible on a light card. Click 'pick a colour below', confirm the armed banner shows, then click a swatch. |
| BandGrid (bands tab — all 5388 slots browser) | `features/colors/BandGrid.tsx` | panel | — | Add modern gate. Replace the jump <input> with the ui TextInput primitive (its [data-slot='input'] shadcn rule makes it a light-safe modern field and escapes the #121315 blanket). Region tabs → ToggleButton(pressed=r.id===region.id, variant='quick'); pager ‹/› | Open window → 'bands' tab. Toggle light mode: confirm the jump field is a light modern input (not near-black), missing-slot placeholders are subtle on white (not dark checker), region tabs show a modern active state with legible counts, and the pager disables  |
| ColorEditor (editor tab — set_color dialog + HSV picker) | `features/colors/ColorEditor.tsx` | form | ✓ | Add modern gate. Fix the one light bug: on .cedit__list ul add modern && 'bg-[var(--sh-console-bg)]... ' — better bg-[var(--pm-panel)] or bg-[var(--sh-btn-fill)] with border-[var(--sh-line-strong)] rounded-[var(--sh-r-sm)] to replace #121315. Convert the R G B | Open window → 'editor'. Toggle light mode: confirm the named-colour list is a LIGHT panel (not near-black) with legible rows and colour dots, the filter/name/hex/channel fields are modern light inputs, the R G B / H S V toggle shows a clear active state, the h |
| SpectrumPanel (spectrum tab — 60 palettes + spectrumany colour list) | `features/colors/SpectrumPanel.tsx` | form | — | Add modern gate. Replace expression/min/max/colours/filter <input>s with ui TextInput, the interpolation <select> with ui Select, and the two checkboxes with ui Checkbox — this gives them the modern light-safe [data-slot] treatment and escapes the #121315 blan | Open window → 'spectrum'. Toggle light mode: confirm ALL fields (expression, filter, min/max, colours, interpolation select) are light modern inputs (not near-black), the palette list hover is a subtle light tint (not a dark band), the mode toggle + go button  |
| RampPanel (ramps tab — ramp_new/ramp_update + volume ramps) | `features/colors/RampPanel.tsx` | form | — | Add modern gate. Recolor the three cards: on .cramp__live/.cramp__editor/.cramp__volume add modern && 'rounded-[var(--sh-r-sm)] border border-[var(--sh-line-strong)] bg-[var(--sh-btn-fill)]' to replace #191b1d. Replace all name/map/stop/volume <input>s with ui | Open window → 'ramps'. Toggle light mode: confirm the three section cards are LIGHT cards (not #191b1d dark boxes), every field (name/map/stops/volume) + the spectrum select are light modern inputs, the notes are legible, and the gradient preview bar renders f |
| SpacePanel (space tab — cmd.space + util.colors element schemes) | `features/colors/ColorsPanel.tsx` | form | ✓ | Add modern gate. Single data fix: .cspace__note #5b6167 → text-[var(--pm-text-dim)]. Optional polish: the two .cspace__head section labels already follow tokens (shadcn.css [class*='__head'] rule uppercases them); leave as-is. No field/button swaps needed — th | Open window → 'space'. Toggle light mode: confirm the gamma input + scheme select are modern light fields, the space buttons (rgb/pymol/greyscale) and 'apply' read as modern buttons, and the explanatory note is legible on white. Click a space button and confir |

## apps/web/src/features/volume

_The Volume Color-Map Editor: a floating dialog (VolumePanel) whose interactive body is a canvas ramp editor (VolumeCanvas), plus a preset/watch header, an inline colour-picker bar, a footer action row, and the modals it triggers. CRITICAL FINDING: volume.css is 100% inside @layer legacy with hardcoded LIGHT colours (#fff canvas/picker, #ffffe1 Qt tooltip, #a11 error, #333 canvas text) and there is NO shadcn.css override for ANY .volpanel__*/.volcanvas__* class — so in the modern theme the panel keeps its classic light styling. The atoms it uses (Button/Select/Checkbox/TextInput via ../../ui) are already theme-aware, but every feature-owned wrapper and the JS-painted canvas are not; and it renders inside DialogWindow chrome (features/dialogs) that is itself still un-modernized classic-light. In modern DARK these read as glaring white/grey/yellow blocks; even in modern LIGHT they are off-token (flat PyMOL greys, wrong radius/border, no tokens) instead of the shadcn card look._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| VolumePanel | `features/volume/VolumePanel.tsx` | panel | ✓ | Add `useTheme` to the `../../ui` import and compute `const modern = useTheme() === 'shadcn'` once. Then thread `cn(legacyClass, modern && '<tailwind>')` through every feature-owned wrapper listed in the sub-entries below (head/status/error/picker/buttons/check | Open the app at the front URL. Pin the modern theme with `?theme=shadcn&appearance=dark` (repeat with `=light`) or click the ThemeToggle in the shell header. A volume object must exist first — in the bottom console load a map and make a volume (e.g. `load <map |
| VolumeSlot | `features/volume/VolumePanel.tsx` | service | — | No visual work. Leave as-is; all styling lives in the child VolumePanel and its regions. | Nothing to see directly; verified transitively when a volume window renders (see VolumePanel). |
| Preset / watch header row | `features/volume/VolumePanel.tsx` | toolbar | ✓ | The <Select> and Reload <Button> already restyle (data-slot='select' rule in shadcn.css §6; Button bare -> FLAT). Convert only the raw wrappers/badges: `.volpanel__head` -> add `modern && 'text-[var(--pm-text)]'` (it only sets font-size:11px today, so mainly e | With the window open in modern dark AND light: the preset dropdown, Reload button, and the source/tracking/stops labels should sit on the panel with token text; open the dropdown and confirm the option list is themed; toggle appearance and confirm the dim labe |
| Status / error strip | `features/volume/VolumePanel.tsx` | other | — | Add `modern && 'text-[var(--sh-danger)]'` to the error div (replacing the hardcoded `#a11` which is nearly unreadable on the dark panel) and `modern && 'text-[var(--pm-text-dim)]'` to the status div (instead of opacity:0.65). Both are raw <div>s, so gate the u | Force an error (e.g. open a volume whose backend read fails, or temporarily point at a bad name) to see the red line — confirm it uses the danger token and is legible in dark. Normal open shows the status line ('histogram via …'); confirm it is dim-but-readabl |
| VolumeCanvas (ramp plot + drag tooltip) | `features/volume/VolumeCanvas.tsx` | overlay | — | Two parts. (1) HOST + TOOLTIP css: give `.volcanvas` `modern && 'border-[var(--sh-btn-border)] rounded-[var(--sh-r-sm)] bg-[var(--pm-panel-alt)]'` (replaces white bg + #cfcfcf border); give `.volcanvas__tip` `modern && 'bg-[var(--pm-panel)] border-[var(--sh-li | Open the window in modern dark; the plot area and its tooltip (drag a stop to show it) must adopt token surfaces — no white block, no yellow tooltip. Exercise add (click), move (drag, tooltip appears), delete (shift/middle click), and ctrl-right-drag zoom band |
| Inline colour-picker bar | `features/volume/VolumePanel.tsx` | form | ✓ | Convert the `.volpanel__picker` container: `modern && 'bg-[var(--pm-panel)] border border-[var(--sh-btn-border)] rounded-[var(--sh-r-sm)] text-[var(--pm-text)]'` (replaces `background:#fff;border:#b0b0b0`). The OK/Cancel buttons already restyle (Button bare -> | Open the window in modern dark, then in the plot LEFT-CLICK an existing stop (no drag) to raise the picker bar. Confirm: the bar is a token surface (not white), the colour swatch fills cleanly (no odd inner padding), and OK/Cancel are modern buttons. Drag the  |
| Footer action row | `features/volume/VolumePanel.tsx` | toolbar | ✓ | All three buttons and the checkbox already restyle via primitives (Button bare -> FLAT; Checkbox data-slot -> accent-color). Only the wrappers are raw: `.volpanel__buttons` needs nothing (flex/gap/wrap layout, no colour). Give `.volpanel__check` `modern && 'te | With the window open in modern dark and light: the three footer buttons render as modern flat buttons, the real-time checkbox shows the accent colour when checked, and the label text is legible. Click Help / Get colors as script to raise the TextDialog (see be |
| DialogWindow (floating window chrome) | `features/dialogs/DialogWindow.tsx` | dialog | ✓ | Out of the volume directory, but flagged because it dominates how the volume panel looks in modern mode. dialogs.css is `@layer legacy` with NO shadcn.css override. To modernize: gate Tailwind on the shell (`bg-[var(--pm-panel)] border-[var(--sh-line-strong)]  | Any dialog demonstrates it. Open the volume window in modern dark: today the outer frame + title bar are grey/gradient while the (converted) interior is dark — the mismatch is the tell. After conversion, frame+title should be a token card in both appearances;  |
| TextDialog (script / help modal) | `features/dialogs/modals.tsx` | dialog | ✓ | Outside features/volume but rendered by it. dialogs.css modal rules are legacy-only. Gate Tailwind on the box (`bg-[var(--pm-panel)] border-[var(--sh-line-strong)] rounded-[var(--sh-r)] shadow-[var(--sh-pop)]`), the scrim (`bg-black/40` is fine, or token), the | In modern dark, click the footer 'Get colors as script' or 'Help' button in the volume window; the modal must be a token card with a themed monospace code box; Copy shows the 'copied' note; Close dismisses. Repeat in light. |
| NumberPrompt (value / range editor modal) | `features/dialogs/modals.tsx` | dialog | ✓ | Same treatment as TextDialog for the shared `.dlgmodal`/`.dlgmodal__box` surfaces. The `.dlgmodal__input` carries its own legacy border/padding AND data-slot='input' (so it double-styles in modern) — reconcile to the token field look (`bg-[var(--sh-btn-fill)]  | In modern dark, right-click a ramp stop (edit value) or left-click a vmin/vmax/amax value box in the plot to raise the prompt; the modal + numeric field must be token-styled (single clean border, readable), OK clamps to the shown min…max range, Cancel/Escape l |

## apps/web/src/features/properties

_The Properties Inspector — a floating dialog (PyMOL properties_dialog port) with a header toolbar (object combo, state/index spinboxes, refresh, follow-pick) over a fixed four-level scrolling property tree whose rows are inline-editable; one component file PropertiesPanel.tsx + properties.css, all classic CSS in @layer legacy._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| PropertiesPanel | `features/properties/PropertiesPanel.tsx` | panel | ✓ | Add `const modern = useTheme()==='shadcn'` at the top of PropertiesPanel and thread it into the className of each container element below via cn(...). For .props itself: `cn('props', modern && 'gap-2')` — trivial, it has no color. The real work is the child el | front is up at http://100.71.244.15:3005. Open with ?theme=shadcn&appearance=dark then again with &appearance=light. Click the 'Properties' button in the fixed bottom-left '.dlglaunch' launcher strip (data-open="properties") to open the window; load an object  |
| PropertiesSlot | `features/properties/PropertiesPanel.tsx` | service | — | Nothing — it renders no DOM. Leave as-is. | N/A directly; verified transitively by the panel appearing when data-open="properties" is clicked. |
| Header toolbar (.props__head) | `features/properties/PropertiesPanel.tsx` | toolbar | ✓ | Gate on modern: `cn('props__head', modern && 'gap-2 text-[12px]')` and on each <label> `cn(modern && 'gap-1.5 text-[var(--pm-text-dim)]')` so the captions become muted on-token text. Keep the 68px number width (fine in modern). Optionally raise gap to match sh | In the open Properties window (both appearances), check the row wraps cleanly, the 'Object/State/Index' captions are legible muted text (not dark-on-dark in dark mode, not washed-out in light), and controls align on one baseline. |
| Object combo (Select, data-props-model) | `features/properties/PropertiesPanel.tsx` | control | ✓ | None required — already primitive + token-driven. Optional: pass a className width if the combo should have a min-width in modern. | Open the combo in both appearances; confirm token fill/border/focus-ring and readable option text; changing it re-reads the tree. |
| State spinbox (TextInput type=number, data-props-state) | `features/properties/PropertiesPanel.tsx` | control | ✓ | None required. Optional polish: if spinner arrows look off on dark, hide them and rely on the min/max clamp, or leave as native. | In both appearances confirm the field shows the modern fill/border/focus-ring; typing a state re-reads the ostate rows. |
| Index spinbox (TextInput type=number, data-props-index) | `features/properties/PropertiesPanel.tsx` | control | ✓ | None required (same as State). | Both appearances: modern field paint + focus ring; changing index updates the Atom-Level rows. |
| Refresh button (IconButton, data-props-refresh) | `features/properties/PropertiesPanel.tsx` | control | ✓ | Works already. Recommended polish for a true modern look: import { RefreshCw } from 'lucide-react' and pass `icon={RefreshCw} iconOnly` (drop the ⟳ child), which renders the lucide glyph in modern only and collapses to a square icon button, matching the rest o | Both appearances: button shows FLAT modern fill + hover; if lucide icon added, confirm classic still shows text-only ⟳ (toggle to classic) and modern shows the icon. |
| Follow-pick checkbox (Checkbox, data-props-follow) + .props__follow label | `features/properties/PropertiesPanel.tsx` | control | ✓ | Checkbox itself needs nothing. Gate the caption: `cn('props__follow', modern && 'text-[var(--pm-text-dim)]')` so 'follow pick' is muted on-token rather than inheriting a possibly dark-on-dark color. | Both appearances: the accent-colored check, legible 'follow pick' caption; toggling it starts/stops the poll (watch network for cmd.index('?pk1')). |
| Busy indicator (.props__busy) | `features/properties/PropertiesPanel.tsx` | other | — | Effectively fine. Optional: `cn('props__busy', modern && 'text-[var(--pm-text-dim)]')` for consistency, or swap the '…' for a small lucide Loader2 spinner in modern. | Trigger a reload (change object/index) and confirm the '…' appears briefly and is legible in both appearances. |
| Error banner (.props__error) | `features/properties/PropertiesPanel.tsx` | other | — | Gate a modern token treatment: `cn('props__error', modern && 'text-[var(--sh-danger)] text-[11px]')`, and optionally give it a soft danger surface: `modern && 'rounded-[var(--sh-r-sm)] bg-[color-mix(in_srgb,var(--sh-danger)_14%,transparent)] px-2 py-1'`. Tailw | Force an error (e.g. type an invalid value into an editable row and blur, or open with no object) and confirm the banner uses the modern crimson and is readable in both appearances. |
| Property tree container (.props__tree) | `features/properties/PropertiesPanel.tsx` | panel | — | Gate: `cn('props__tree', modern && 'rounded-[var(--sh-r-sm)] border border-[var(--pm-line)] bg-[var(--pm-panel-alt)]')` (or `bg-transparent` if it should sit directly on the panel frost). This replaces #fff and #c8c8c8 with tokens that flip correctly between d | Both appearances: the tree body must match the panel — dark translucent/paneled in dark, light in light — NOT a bright white box; the border is a subtle on-token hairline; scrollbars already themed by shadcn.css §12. |
| Section header (.props__section-label) | `features/properties/PropertiesPanel.tsx` | other | — | Gate for the modern section-header look: `cn('props__section-label', modern && 'uppercase tracking-[0.08em] text-[10px] font-semibold text-[var(--pm-text-dim)]')` to match the rest of the modern chrome's section labels. Optional but makes the tree read as mode | Both appearances: the four level titles read as muted tracked uppercase labels (modern) and are legible; classic stays plain bold. |
| Group header (.props__group-label) | `features/properties/PropertiesPanel.tsx` | other | — | Optional: `cn('props__group-label', modern && 'text-[var(--pm-text-dim)] opacity-100 text-[10.5px]')` to switch from opacity-dimming to an explicit muted token. Low priority. | Both appearances: group titles are clearly a level below section titles and legible. |
| Property row (.props__row / .props__row.is-selected) | `features/properties/PropertiesPanel.tsx` | control | — | Compute `modern` and build the className in JSX as `cn('props__row', selected===id && 'is-selected', modern && 'rounded-[var(--sh-r-sm)] hover:bg-[var(--sh-btn-hover)]', modern && selected===id && 'bg-[var(--sh-accent-soft)] text-[var(--pm-text-bright)]')`. Th | Both appearances: click rows — selection is an on-accent soft tint (not pale classic blue), hover shows a subtle fill, and Delete on a selected editable row still unsets it. |
| Editable value field (.props__value, data-props-value) | `features/properties/PropertiesPanel.tsx` | control | ✓ | No color work needed. Optional density tweak for the dense tree: gate a tighter pad/height, e.g. `cn('props__value', modern && 'py-0.5')`, if the taller inputs make the tree feel loose. Optionally force mono for aligned numeric data: `modern && 'font-[family-n | Both appearances: value inputs show the modern field paint + focus ring; edit a settable row (e.g. sphere_scale) and blur — value commits and the tree reloads; check row density is acceptable. |
| Read-only value (.props__ro) | `features/properties/PropertiesPanel.tsx` | other | — | Optional: `cn('props__ro', modern && 'text-[var(--pm-text-dim)] opacity-100')` to use an explicit muted token instead of opacity. Low priority. | Both appearances: read-only rows (e.g. oneletter, identifiers) show muted mono text distinct from editable fields. |
| Unavailable marker (.props__unavailable) | `features/properties/PropertiesPanel.tsx` | other | — | There is no amber token, so gate an explicit modern amber that works on both: `cn('props__unavailable', modern && 'text-[#d08700]')` (or introduce a --sh-warn token in shadcn.css §1 for dark+light and reference it). Utilities override the legacy #a06000. | Find/force an unavailable row and confirm the 'unavailable' text is a legible warning color in both appearances (not muddy brown on dark). |
| Empty-group placeholder (.props__empty) | `features/properties/PropertiesPanel.tsx` | other | — | Effectively fine. Optional: `cn('props__empty', modern && 'text-[var(--pm-text-dim)]')` for consistency. Lowest priority. | Both appearances: a group with no rows shows a faint '—' that is legible but clearly secondary. |

## apps/web/src/features/texteditor

_The Text Editor overlay — a pymolrc/script editor (Qt QMainWindow port): File menu (Open/Save/Save as/Run), exclusive Python/PML/Plain syntax radios, a monospace textarea-over-pre highlighted editor, a status bar with a bridge-vs-browser access badge, inline error/status/note banners, an active-pymolrc chooser, and a Save-changes guard modal._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| TextEditorSlot | `features/texteditor/TextEditorPanel.tsx` | service | — | No change. It only fans out panels; all styling lives in TextEditorPanel. | Not directly visible. Confirmed indirectly: opening two editors (File ▸ Edit pymolrc plus the overlay-launcher SquarePen) should mount two independent panels. |
| TextEditorPanel (root .txted) | `features/texteditor/TextEditorPanel.tsx` | panel | ✓ | Import useTheme + cn from '../../ui'; add `const modern = useTheme() === 'shadcn'`. The .txted container itself needs no utilities (pure flex). Do the real work on the children below, each gated `cn('txted__x', modern && '…')`. Separately flag to the dialogs o | Open http://100.71.244.15:3005/?theme=shadcn&appearance=dark. Open the editor via the menubar File ▸ Edit pymolrc, or the floating overlay-launcher pill (pencil/SquarePen icon). Repeat with &appearance=light and with ?theme=classic to confirm classic is byte-i |
| Menu / action toolbar (.txted__menu) | `features/texteditor/TextEditorPanel.tsx` | toolbar | ✓ | Gate on modern: give .txted__menu `modern && 'gap-2 text-[11px] items-center'` (mostly parity). Give .txted__menu-label `modern && 'font-semibold uppercase tracking-wide text-[10px] text-pm-text-dim'` to match the section-header treatment already in shadcn.css | With ?theme=shadcn open the editor; check the top row: labels are muted uppercase, the separators are faint hairlines matching other modern seams in both dark and light, buttons/inputs align on one baseline. |
| File action buttons (Open / Save / Save as) | `features/texteditor/TextEditorPanel.tsx` | control | ✓ | The Button primitive needs nothing. Tokenize the kbd: add `modern && 'border-pm-line text-pm-text-dim rounded-[3px]'` to each <kbd> (or add a shared className). Because kbd sits inside .txted__menu (legacy layer) the utility wins. Both appearances then track - | ?theme=shadcn: the three File buttons render as flat bordered chips; the Ctrl+… hints are quiet, with a hairline border that matches the panel seams in both dark and light. Click Open → browser picker or Open-file prompt; edit + Ctrl+S. |
| Run button (state-gated) | `features/texteditor/TextEditorPanel.tsx` | control | ✓ | No structural change. Optional: add a lucide `icon={Play}` for modern parity. Confirm the disabled look is legible in light mode (opacity-45 on a --sh-btn-fill chip) — if too faint, swap to a token-based disabled treatment rather than pure opacity. | ?theme=shadcn: with no file loaded Run is visibly disabled; open a server file (needs the bridge fs) so access becomes 'bridge fs' and Run enables. Check disabled contrast in both appearances. |
| Syntax radio group (Python / PML / Plain Text) | `features/texteditor/TextEditorPanel.tsx` | form | — | Either (a) minimal: add `data-slot="radio"` to each input and a shadcn.css rule `[data-slot='radio']{accent-color:var(--pm-accent)}` alongside the existing checkbox/slider rule so light+dark both pick up --pm-accent; or (b) preferred: add a Radio primitive to  | ?theme=shadcn (dark AND light): the three radios show the indigo accent when selected, not the OS blue; clicking toggles exclusively and re-highlights the editor (PML keywords vs Python vs plain). |
| Font size number input | `features/texteditor/TextEditorPanel.tsx` | control | ✓ | Largely done via the primitive. Optionally move the 46px width to a `modern && 'w-12'` utility for consistency and drop reliance on the legacy .txted__radio input rule. No color work needed — it already reads --sh-btn-fill/--sh-btn-border/--pm-text-bright, whi | ?theme=shadcn dark+light: the Font field shows the modern bordered fill matching other inputs; typing 8–24 changes the editor text size; out-of-range clamps. |
| Status bar (.txted__bar: path + dirty + access badge) | `features/texteditor/TextEditorPanel.tsx` | toolbar | — | Gate: .txted__path `modern && 'font-mono text-pm-text-dim'`. Replace hardcoded #a06000 on .txted__dirty with a token — either `modern && 'text-pm-caption'` (green 'changed' accent) or an amber design token; do NOT keep #a06000 or it fails in dark. Optionally p | ?theme=shadcn dark+light: load a file — path shows in mono muted; edit it — '• modified' appears in a legible accent (readable in BOTH appearances, not brown-on-dark); the right-aligned badge cycles probing…→bridge fs/browser fs and stays legible. |
| Inline status / error messages (.txted__status, .txted__error) | `features/texteditor/TextEditorPanel.tsx` | other | — | Gate .txted__error `modern && 'text-[var(--sh-danger)]'` (the modern danger token, which is defined for both dark #f5455c and light #e11d48). Leave .txted__status as opacity/inherit or add `modern && 'text-pm-text-dim'`. Both then read correctly in light and d | ?theme=shadcn dark+light: trigger an error (Open a nonexistent server path, or open the editor with no bridge fs) → the error line is the modern danger red and readable in both appearances; a successful Save shows the muted status line. |
| pymolrc 'restart to apply' note (.txted__note) | `features/texteditor/TextEditorPanel.tsx` | other | — | Gate the whole note: `modern && 'bg-[var(--sh-accent-soft)] text-pm-text-dim border-t border-pm-line rounded-[var(--sh-r-sm)]'` (a calm tinted info box in the modern palette) — or introduce a dedicated warning token. Must NOT keep #fff4d6/#6a4a00 under modern. | ?theme=shadcn dark+light: open File ▸ Edit pymolrc (or any *pymolrc file) — the note shows as a subtle tinted info strip that fits the modern palette in BOTH dark and light; confirm classic (?theme=classic) still shows the original cream #fff4d6 note. |
| Active-pymolrc chooser (.txted__note > Select) | `features/texteditor/TextEditorPanel.tsx` | control | ✓ | Fix follows the note fix — once .txted__note is tokenized, the Select and label inherit correct colors. No change to the Select itself (primitive handles it). Optionally add `modern && 'text-pm-text'` to the label for explicitness. | Reachable only with a multi-rc host (>1 active pymolrc). If unavailable, verify visually by confirming the Select primitive elsewhere (Font input row uses the same field paint). In ?theme=shadcn dark+light the dropdown should show the modern bordered fill and  |
| Code editor surface (.txted__editor: textarea over highlighted pre) | `features/texteditor/TextEditorPanel.tsx` | other | — | This needs a real modern editor treatment, not just tokenized BEM. Steps: (1) gate .txted__editor `modern && 'bg-pm-panel-alt border border-pm-line rounded-[var(--sh-r-sm)]'`; (2) .txted__pre `modern && 'text-pm-text'`; (3) .txted__area `modern && 'caret-[var( | ?theme=shadcn dark: type Python and PML — the editor is a dark card matching the panel (NOT a white box), text and every token color is readable, caret is indigo, selection is a soft indigo wash, scrolling keeps the highlight aligned. Switch appearance=light:  |
| ConfirmPrompt 'Save changes?' modal (rendered by the panel) | `features/dialogs/modals.tsx` | dialog | ✓ | Out of scope for texteditor's own files — flag to the dialogs owner: tokenize .dlgmodal__box (bg --sh-panel-frost / --pm-panel, border --sh-line-strong, radius --sh-r, shadow --sh-pop) under [data-ui-theme='shadcn'] so the guard matches the modern palette in b | ?theme=shadcn: edit the buffer (make it dirty) then click Open (or close the window) → the Yes/No/Cancel guard appears. Today it renders as a light classic card even in modern dark — note this as the dialogs dependency; after dialogs is converted, re-check it  |
| DialogWindow chrome (surrounding frame) | `features/dialogs/DialogWindow.tsx` | panel | ✓ | Out of scope for texteditor's files — flag to the dialogs owner: add [data-ui-theme='shadcn'] .dlgwin{...} using --sh-panel-frost/--sh-blur/--sh-line-strong/--sh-r/--sh-pop and a token titlebar, so floating dialogs match the modern panel treatment already appl | ?theme=shadcn dark: open the editor and observe the window frame is still classic light-gray with a gradient titlebar (the gap). This is the top dependency for the texteditor conversion to look finished; verify after dialogs is modernized that the frame matche |

## apps/web/src/features/compute

_The "Compute" overlay panel (WP-24/B9): runs pymol.util scalar/selection/action helpers against a shared selection, with a destructive-confirm flow, a per-residue SASA table, and captured PyMOL diagnostics. One React file (ComputePanel.tsx) + one legacy-layer stylesheet (compute.css); metrics.ts is pure data, register.ts is wiring._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ComputePanel (root overlay panel) | `features/compute/ComputePanel.tsx` | panel | — | Cleanest: render the root through the Panel primitive — change `<div className="compute">` to `<Panel className="compute">` (import Panel from '../../ui'). That stamps data-slot="panel" so shadcn.css §8 gives it --sh-panel-frost + var(--sh-blur) + 1px var(--sh | Front is up (http 200) at http://100.71.244.15:3005. Open with ?theme=shadcn&appearance=dark then ?theme=shadcn&appearance=light (or use the header ThemeToggle). Click the "Compute" pill in the floating overlay-launcher toolbar to open the panel. Check the pan |
| Panel title bar | `features/compute/ComputePanel.tsx` | toolbar | — | Optional polish. To match modern section headers add, gated to modern, `uppercase tracking-[0.08em] font-semibold text-[10px] text-[var(--pm-text-dim)] bg-transparent px-3 py-2`. Keep it flush with the frosted root (drop the solid --pm-panel-alt fill in modern | Open the Compute panel in modern dark + light; confirm the title reads as a calm muted header, not a heavy filled bar, and contrast is fine on the light card. |
| Selection field | `features/compute/ComputePanel.tsx` | form | ✓ | Essentially done via the primitive. Optional: the shadcn rule forces sans on the selection expression; if you prefer mono for a selection string, add modern-gated `font-mono` on the TextInput className. No light-mode fix required. | Open panel; the Selection box should be a rounded modern field with a focus ring (outline var(--sh-focus-ring)) in both appearances; label legible on light. |
| Metric run button (compute__btn, incl. is-danger) | `features/compute/ComputePanel.tsx` | control | ✓ | On the compute__btn className append, gated to modern: `justify-start text-left` to restore left alignment. For the danger row add modern-gated danger utilities keyed off the class, e.g. `[&.is-danger]:text-[var(--sh-danger)] [&.is-danger]:border-[color-mix(in | Open panel in modern; buttons should be full-width but LEFT-aligned. Confirm "Protein vacuum ESP" is visibly red/destructive in both dark and light, while normal metrics are neutral. Disabled state (press any button, others grey out) should dim via disabled:op |
| ParamField (per-metric argument inputs) | `features/compute/ComputePanel.tsx` | form | ✓ | In compute.css replace every `var(--fg-dim, #9aa)` with `var(--pm-text-dim)` (defined and appearance-aware in both classic and modern) — this is a token fix, safe for classic too since classic also defines --pm-text-dim. Field widths can stay; optionally bump  | Open panel; scroll to metrics with args (Relative SASA per residue, Interchain distances, Align all objects to target, Copy force-field parameters). In light mode confirm the little field labels are readable, not washed-out grey; checkboxes show the indigo acc |
| Result cell (compute__val / is-error / compute__off) | `features/compute/ComputePanel.tsx` | other | — | The cell isn't a primitive, so gate a modern utility on it. Build the className with cn and, when modern, add `[&.is-error]:text-[var(--sh-danger)]` (or branch: `row?.error && modern && 'text-[var(--sh-danger)]'`). Leave the legacy .is-error green rule for cla | Open panel, set Selection to an invalid expression (e.g. `zzz`) and press a scalar metric (Mass) to force an error; confirm the error text is red in modern dark AND light (not green). Valid runs (Mass, Molecular surface area) show bright numeric results. |
| Destructive-confirm dialog (compute__confirm) | `features/compute/ComputePanel.tsx` | dialog | ✓ | Two options. (A) Keep the bespoke box, fix semantics: gate modern utilities on the container `rounded-[var(--sh-r-sm)] border-[var(--sh-danger)] bg-[var(--sh-panel-alt)] p-2` and on the warning text `text-[var(--sh-danger)]`; fix the confirm button per the met | Open panel, press "Protein vacuum ESP" (destructive) and separately "B-factors to VDW radii" or "Relative SASA per residue" (warning). Confirm the box + warning + primary button are RED/destructive in modern dark and light (not green), and Cancel is neutral. I |
| SasaTable — per-residue exposure list | `features/compute/ComputePanel.tsx` | panel | ✓ | In compute.css swap the undefined-token fallbacks for real tokens: `.compute__sasa-sele` color -> var(--pm-accent); `.compute__sasa-bar > span` background -> var(--pm-accent); `.compute__sasa-bar` background -> var(--sh-btn-fill) (or var(--pm-line)) so the tra | Load a structure first, then run "Relative SASA per residue" (default selection polymer). In modern light confirm: residue links are the indigo accent and readable, bars have a light-tinted track (not a black slab) with an indigo fill, head text is legible, ra |
| Diagnostics disclosure (compute__diag / diaglines) | `features/compute/ComputePanel.tsx` | other | — | In compute.css: `.compute__diag`/summary color -> var(--pm-text-dim). For `.compute__diaglines` decide the intent: (a) match the app's terminal (stays dark in light mode intentionally) -> background var(--sh-console-bg), color var(--pm-text), font var(--pm-fon | Run "Protein vacuum ESP" (confirm the destructive dialog first) on a loaded structure; ~1s later the diagnostics disclosure fills in. In modern light confirm the summary text is readable and the <pre> block is either a coherent dark terminal or a light recesse |
| Inline metric note (compute__note-inline) | `features/compute/ComputePanel.tsx` | other | — | Low priority. Either leave the amber (acceptable in both) or centralise a warn token: define --sh-warn in shadcn.css (e.g. #d08a2e light / #d89b3a dark) and replace `var(--warn, #d89b3a)` here and in .compute__warn-inline / .is-raw. No structural/Tailwind chan | Open panel; the notes under Interchain distances / Align all objects to target / Copy force-field parameters should be readable amber in both modern appearances. |
| Footer note (compute__note) | `features/compute/ComputePanel.tsx` | other | — | None required for correctness. Optional: soften the hard border-top to `border-top:1px solid var(--pm-line)` in modern for the calmer modern seam (via a shadcn.css `[data-ui-theme='shadcn'] .compute__note` rule or a modern-gated utility). Cosmetic only. | Open panel; footer text legible and low-emphasis on both the dark and the light card; the divider above it is a hairline, not a heavy edge. |

## apps/web/src/features/plugin-manager

_Read-mostly Plugin Manager overlay (mirrors PyMOL managergui_qt) — four tabs (Installed, Legacy Plugins, Settings, Startup Paths) plus a shared inline write-confirmation band; only per-plugin autoload, two prefs, and the user startup-path list are writable._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| PluginManager (root overlay panel) | `features/plugin-manager/PluginManager.tsx` | panel | ✓ | Wrap the returned subtree in the Panel primitive (`<Panel className="plugmgr">`) or add modern-gated card utilities so the overlay picks up frost + 1px var(--sh-line-strong) border + var(--sh-r) radius + var(--sh-pop) shadow. Give `.plugmgr` its own modern bac | Open http://100.71.244.15:3005 with ?theme=shadcn&appearance=dark then &appearance=light (or use the header ThemeToggle). Click the 'Plugins' pill in the floating overlay-launcher to open it. Confirm the panel is a rounded frosted card (not a hard-edged box).  |
| Tab strip + rescan toolbar | `features/plugin-manager/PluginManager.tsx` | toolbar | ✓ | Convert the four tabs from `Button` to `ToggleButton pressed={tab === t.id}` and drop the manual `is-on` className — ToggleButton emits the modern accent 'on' fill (data-[state=on]:bg-[var(--pm-accent)]) so the active tab is marked again; keep role=tab and ari | In the open panel (modern dark AND light) click each tab and confirm exactly one tab is visibly highlighted and the highlight follows your click. Confirm the classic theme still shows the original pressed-tab look (toggle ThemeToggle to classic). Click 'rescan |
| Installed Plugins tab (plugin table) | `features/plugin-manager/PluginManager.tsx` | panel | ✓ | Modern-gated utilities on the table: give th an uppercase tracked muted header (text-[10px] uppercase tracking-wide text-[var(--pm-text-dim)]), swap th/row borders from --pm-edge to the softer --pm-line, add row padding (py-1.5 px-2) and a subtle hover (hover: | Open the panel on the Installed Plugins tab in modern dark + light. Confirm the table header reads as a muted modern header, rows have breathing room and a hover, the mono file path truncates from the left, and the note blocks read as calm cards. Compare again |
| Autoload checkbox (per-plugin, writable) | `features/plugin-manager/PluginManager.tsx` | control | ✓ | Largely done via the primitive — no per-checkbox change required. Optionally verify the accent-color contrast is adequate on the light --pm-panel and that the disabled (saving) state is visually distinct; if desired add a modern-gated cursor-not-allowed/opacit | On Installed Plugins, toggle a plugin's Enabled box in modern light + dark: the check fill should be the indigo --pm-accent, the box should sit centered in its column, and while a write is in flight the boxes should read as disabled. |
| Settings tab (preferences) | `features/plugin-manager/PluginManager.tsx` | form | ✓ | Keep the grid but give it modern-gated spacing (gap-y-1.5 gap-x-4, py-1). Turn the two prefs into row cards or at least add hover affordance on the label. Consider promoting these two booleans from Checkbox to the Switch primitive (role=switch, shadcn.css alre | Open Settings in modern dark + light. Toggle 'verbose' — the Confirm band should appear; the current-value <dd> should update after Apply. Toggle 'instantsave' OFF and confirm the Confirm band's text switches to the 'session only, file not touched' wording. Fo |
| Confirm (inline write-confirmation band) | `features/plugin-manager/PluginManager.tsx` | form | ✓ | Add modern-gated utilities to `.plugmgr__confirm`: rounded-[var(--sh-r-sm)], border-[var(--pm-line)], a subtle accent-tinted background (bg-[var(--sh-accent-soft)] or a warn tint) to read as a call-to-action, and roomier padding (p-3). The Apply/Cancel buttons | On Settings or Startup Paths, stage an edit to make the band appear (modern dark + light). Confirm the band reads as an attention card with a clear primary Apply vs secondary Cancel, its text is legible, and the buttons disable/show 'writing…' during a write. |
| Startup Paths tab (editable path list + add row + fixed list) | `features/plugin-manager/PluginManager.tsx` | form | ✓ | Convert the ↑ ↓ ✕ Buttons to the IconButton primitive with lucide icons (ArrowUp, ArrowDown, X) so they render as proper square modern icon buttons instead of raw glyphs (keep aria-labels + data-plugin-path-* attrs and the text glyph in an sr span for tests).  | Open Startup Paths in modern dark + light. Confirm the ↑↓✕ controls are square modern icon buttons (disabled up on first row, disabled down on last), the add TextInput matches the modern field look, staging an add/remove/reorder shows the Confirm band, and the |
| LegacyPlugins tab | `features/plugin-manager/LegacyPlugins.tsx` | panel | ✓ | Replace every undefined legacy token in the LegacyPlugins block of plugin-manager.css with the app's theme-aware --pm-* tokens (all defined for dark+light): `.plugmgr__legacyhead` → text-[var(--pm-text-dim)]; `.plugmgr__legacymenu` → color var(--pm-text-bright | Open the Legacy Plugins tab in modern LIGHT mode first (?appearance=light): confirm the menu-group labels are readable (not white-on-white), the separators are hairlines not heavy bars, and — after clicking any leaf to produce a result — the result banner is a |
| Legacy menu leaf button | `features/plugin-manager/LegacyPlugins.tsx` | control | ✓ | Decide the modern intent: if these should read as menu items, pass a GHOST-style variant (variant menubar) or add modern-gated ghost utilities (text-[var(--pm-text)] hover:bg-[var(--sh-btn-hover)] rounded-[var(--sh-r-sm)] justify-start) so leaves look like lis | On the Legacy tab (if any leaf is registered) hover and click a leaf in modern dark + light: it should read as an aligned menu-row (not a stray filled button), highlight on hover, and produce a result banner. Confirm classic still shows the accent text-link st |
| Legacy result banner | `features/plugin-manager/LegacyPlugins.tsx` | other | — | Retoken to theme-aware surfaces: background var(--sh-btn-fill) (or a success/danger tint), color var(--pm-text-dim), add 1px var(--pm-line) border and rounded-[var(--sh-r-sm)]; keep is-error on var(--sh-danger). Optionally split success vs error into a subtle  | Trigger a leaf click on the Legacy tab in modern light mode and confirm the resulting status banner is a light muted card that fits the panel (no dark rectangle); trigger a failure (or observe the refused-Qt path) and confirm the error variant is a legible dan |

## features/apbs

_APBS Electrostatics deferred-feature stub: a single read-only overlay panel that probes the machine for the apbs/pdb2pqr binaries and offers copy-paste command snippets; one panel component + an internal CopyButton, styled entirely by legacy BEM CSS (apbs.css) reading --pm-* tokens._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ApbsPanel | `features/apbs/ApbsPanel.tsx` | panel | ✓ | The shell is a raw <div className="apbs"> — it does NOT use the Panel primitive and has no data-slot, so it never inherits shadcn.css's card treatment (frost/radius/shadow). Follow the Button.tsx gating pattern: add `import { useTheme } from '../../ui'` and `c | Ensure front is up (`./restart front`; it returns HTTP 200 at http://100.71.244.15:3005). Open http://100.71.244.15:3005/?theme=shadcn&appearance=light (or use the ThemeToggle in the shell header to pick Modern + Light). Open the panel via the overlay-launcher |
| CopyButton | `features/apbs/ApbsPanel.tsx` | control | ✓ | Already imports and renders <Button variant="bare"> from '../../ui', so in the modern theme it already receives the FLAT Tailwind treatment (h-7 px-3 rounded border bg-[var(--sh-btn-fill)] etc.) from MODERN_VARIANT.bare — visually it is mostly modern already.  | In modern light (see ApbsPanel verify) open the panel and confirm both buttons render as flat shadcn buttons (soft translucent fill, 6px radius, subtle border, hover fill) rather than the grey classic block, sit with even height, and that clicking one flips on |
| ProbeStatusList | `features/apbs/ApbsPanel.tsx` | other | — | Gate Tailwind on the modern flag. .apbs__list -> `rounded-[var(--sh-r-sm)] border border-[var(--sh-btn-border)] bg-[var(--sh-btn-fill)] px-3 py-2 font-mono text-[11px] leading-relaxed list-none` — crucially replace bg var(--pm-bg) (which is nearly white in lig | In modern light, open the panel: the two-line program-status block should read as a clearly bounded, faintly tinted inset card (not a near-invisible box), with 'not found (tried ...)' rows dimmed and any found path emphasised. Because no binaries are installed |
| ScriptBlock | `features/apbs/ApbsPanel.tsx` | other | — | Gate Tailwind on the modern flag. .apbs__script -> `rounded-[var(--sh-r-sm)] border border-[var(--sh-btn-border)] bg-[var(--sh-btn-fill)] px-3 py-2 font-mono text-[11px] leading-relaxed text-[var(--pm-text)] overflow-x-auto whitespace-pre` — again swap bg var( | In modern light, open the panel: both command snippets should sit in clearly delimited, faintly tinted monospace code cards (not near-invisible), horizontally scroll if narrow, and the inline code tokens in the surrounding sentences should read as subtle chips |

## apps/web/src/features/render

_The Draw/Ray render dialog — a two-page (setup/result) size+DPI form living in the `viewport` region, collapsed to a "Ray / Draw" tab until opened; renders an image via draw/ray into the viewport, then saves/copies it. All styling is in features/render/render.css inside `@layer legacy`, and it is 100% CSS-token-driven (no hardcoded colours), so light-mode contrast is never broken — the modern gaps are polish (no card frost/shadow), a lost primary-button accent, and dated 11px/mono typography._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| RenderDialog (root feature panel) | `features/render/RenderDialog.tsx` | panel | ✓ | Add `const modern = useTheme() === 'shadcn'` (useTheme is exported from ../../ui). Easiest correct fix: swap the raw `<div className="render">` for the `<Panel className="render">` primitive so it inherits `[data-slot='panel']` (frost/blur/border/radius/shadow | Ensure front is up (http://100.71.244.15:3005; `./restart front`). Open with theme forced: http://100.71.244.15:3005/?theme=shadcn&appearance=dark (and again &appearance=light), or flip the ThemeToggle in the shell header. The panel appears collapsed as a 'Ray |
| Collapsed 'Ray / Draw' tab | `features/render/RenderDialog.tsx` | control | ✓ | Mostly already modern via the bare FLAT variant. Optional polish: pass a lucide icon (`icon={Camera}` or similar) so the tab shows an icon in modern only (classic stays text), and gate `modern && 'text-[11px]'` if the h-7 button feels large for a corner tab. K | In modern theme, before expanding, confirm the top-right 'Ray / Draw' pill is a flat bordered button (not the classic grey chip) and hover fills with --sh-btn-hover in both light and dark. |
| Panel title bar + close button | `features/render/RenderDialog.tsx` | toolbar | ✓ | Title: optionally swap the raw div for `<PanelHeader className="render__title">` and gate the shadcn section-header look `modern && 'border-b border-[var(--pm-line)] bg-transparent px-3 py-2 text-[10px] font-semibold uppercase tracking-[0.08em] text-[var(--pm- | Expand the panel in modern (both appearances). Confirm the header reads as a muted uppercase label and the close control is a borderless ghost 'x' that tints on hover, NOT a filled box. Click it to confirm it collapses back to the tab. |
| Setup page — size/DPI grid form | `features/render/RenderDialog.tsx` | form | ✓ | Fields need no work (primitives already carry the modern field treatment). To finish the look, gate the unit labels to the sans UI font in modern: on `.render__unit` add `cn('render__unit', modern && 'font-[var(--pm-font-ui)] text-[var(--pm-text-dim)]')` (keep | Expand → setup page. In modern dark AND light, confirm the six number fields and the inch/cm select have the rounded shadcn field style (soft fill, focus ring), readable text, and correct contrast on the panel. Type a width and confirm the units column recompu |
| Setup page — options row (lock / transparent / use-viewport) | `features/render/RenderDialog.tsx` | form | ✓ | Functionally correct already. Polish only: gate label alignment `modern && 'inline-flex items-center gap-1.5'` on each `<label>` so the checkbox and caption sit on a clean baseline, and optionally trade Checkbox for the Switch primitive (data-slot='switch') if | On the setup page in modern (light+dark), confirm both checkboxes show an indigo accent tick when checked, labels are aligned, and 'use viewport size' is a flat modern button. Load a molecule then click it and confirm width/height adopt the viewport size. |
| Setup page — Draw / Ray action buttons | `features/render/RenderDialog.tsx` | toolbar | ✓ | Give Ray a real accent in modern, mirroring the ToggleButton on-state used elsewhere: on the Ray button use `cn('render__btn render__btn--primary', modern && 'border-transparent bg-[var(--pm-accent)] text-[var(--sh-accent-text)] hover:bg-[var(--pm-accent)] hov | Setup page, modern dark AND light: confirm Ray is a filled indigo accent button clearly distinct from the neutral Draw button, and both dim (opacity) while a render runs. Click Ray with a molecule loaded and confirm it advances to the result page and the butto |
| Result page — note, save-path field, Save/Copy/Back | `features/render/RenderDialog.tsx` | form | ✓ | No functional fix needed. Polish: gate `.render__note` to the sans UI font in modern (`modern && 'font-[var(--pm-font-ui)]'`) so the paragraph matches modern chrome, and optionally add lucide icons to Save (Save/Download) and Copy (Copy) via the `icon` prop, a | Trigger a render (Ray or Draw with a molecule loaded) to reach the result page. In modern light+dark confirm: the note paragraph is legible, the path field is a modern rounded input with a visible placeholder, and Save/Copy/Back are consistent flat buttons. En |
| Status line | `features/render/RenderDialog.tsx` | other | — | Gate to sans + a subtle top divider in modern: `cn('render__status', modern && 'border-t border-[var(--pm-line)] bg-transparent px-3 py-1.5 font-[var(--pm-font-ui)] text-[var(--pm-text-dim)]')`. Optional: when the message is an error, tint with --sh-danger (`t | Force an error to see it: click Save with an empty path ('enter a path to save to'), or Ray with no molecule. In modern light+dark confirm the status strip sits under a hairline divider, is legible, and (if you add danger tinting) errors read red while success |

## apps/web/src/features/shortcuts

_The mouse/keyboard configuration surface (WP-23): a portalled "Mouse & Keys…" launcher in the internal-gui column, a Mouse configuration modal, and a 700x700 Keyboard Shortcut Menu editor with a filterable/editable bindings table, action bar, Create-New form, and overwrite-confirm dialog. Renders 2 source files (ShortcutEditor.tsx, ShortcutsService.tsx) + shortcuts.css; all styling is legacy-layer BEM (.scmodal__*, .butmode__config) on --pm-* tokens with a few hardcoded rgb() values; nothing imports the ../../ui primitive layer yet._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| ShortcutsService (service host) | `features/shortcuts/ShortcutsService.tsx` | service | — | Nothing to restyle in the wrapper itself except to make its two children modern (see 'Mouse & Keys launcher' and 'Mouse configuration modal'). Add `const modern = useTheme()==='shadcn'` here so it can pass gated Tailwind to the raw elements it owns. Keep creat | Open http://100.71.244.15:3005/?theme=shadcn&appearance=dark, ensure the right internal-gui column is visible (internal_gui on), scroll to the very bottom of that column — the ButMode block + a small left-aligned 'Mouse & Keys…' button appear there. Repeat wit |
| Mouse & Keys launcher button (.butmode__config) | `features/shortcuts/ShortcutsService.tsx` | control | — | Replace the raw <button> with the Button primitive: <Button variant="bare" className={cn('butmode__config', modern && 'w-full justify-start rounded-none')} icon={Mouse or Keyboard from lucide} .../>. Because .butmode__config is legacy-layer, adding the FLAT/GH | In the internal-gui column bottom, hover the 'Mouse & Keys…' strip (dark + light) — confirm modern gives a rounded/filled hover, not the classic flat mono strip; click opens the Mouse configuration modal. |
| Mouse configuration modal (scmodal--wide wrapper) | `features/shortcuts/ShortcutsService.tsx` | dialog | — | 1) Gate Tailwind on the shell: on .scmodal add `modern && 'bg-black/50'` (or keep scrim); on .scmodal__box--wide add `modern && 'rounded-[8px] border-[var(--sh-line-strong)] bg-[var(--sh-panel-frost)] shadow-[var(--sh-pop)] backdrop-blur-2xl'` to replace the s | Click 'Mouse & Keys…' -> the wide modal opens centered over a scrim. In light mode confirm the box uses a light panel with a soft (not black-heavy) shadow and rounded corners; the × and 'Keyboard Shortcut Menu…' look like modern buttons. Click the footer butto |
| ShortcutEditor (Keyboard Shortcut Menu dialog) | `features/shortcuts/ShortcutEditor.tsx` | dialog | — | Same shell treatment as the mouse modal: add `const modern = useTheme()==='shadcn'` and gate Tailwind on .scmodal__box -> `modern && 'rounded-[8px] border-[var(--sh-line-strong)] bg-[var(--sh-panel-frost)] shadow-[var(--sh-pop)] backdrop-blur-2xl backdrop-satu | Open the mouse modal then click 'Keyboard Shortcut Menu…' -> the 700x700 editor opens. Toggle appearance=light: the box must be a light panel with soft shadow + rounded corners, title/close legible. Press Esc/× closes. |
| Shortcut toolbar (Filter + Refresh) | `features/shortcuts/ShortcutEditor.tsx` | toolbar | — | Replace the filter input with <TextInput className="scmodal__filter flex-1" .../> (data-slot=input gives it the modern field treatment: radius + focus ring, auto light/dark). Replace Refresh with <Button variant="bare" icon={RefreshCw}>. Wrap the row in <Toolb | In the editor, type in the Filter box — rows narrow live; confirm modern input has rounded corners + accent focus ring in dark AND light. Click Refresh (note: with the bridge panel absent it falls back to the seeded default table and writes a message to the st |
| Shortcut bindings table (Key / Command / Description) | `features/shortcuts/ShortcutEditor.tsx` | panel | — | Keep the <table>/<tr>/<td> DOM, the .scmodal__table/.scmodal__key/.is-user/.is-selected classes and the .scmodal__edit input class + data-testids (all pinned by tests). Fix light mode and modernize by adding gated Tailwind (or a scoped [data-ui-theme='shadcn'] | In light mode: confirm every row has a visible bottom hairline (this is the headline light-mode bug). Click a row -> selection tint should read as the accent (indigo), not a stray purple. Double-click a Command cell -> inline editor appears with an accent-bord |
| Action button bar (Create/Delete/Reset/Reset All/Save) | `features/shortcuts/ShortcutEditor.tsx` | toolbar | — | Replace all five raw <button>s with the Button primitive (variant="bare" for a FLAT filled modern button; Save could be given accent emphasis). Add lucide icons per action (Plus, Trash2, RotateCcw, RotateCcw, Save). The primitive already handles disabled:opaci | With no rows selected, confirm 'Delete Selected' + 'Reset Selected' are visibly disabled (modern: ~45% opacity) in dark AND light. Select a row to enable them. Hover each — modern buttons should get the FLAT hover (border-strong + bg-hover). Click Save and rea |
| Status line (.scmodal__status) | `features/shortcuts/ShortcutEditor.tsx` | other | — | Low effort: keep the element + data-testid. Optionally gate `modern && 'text-[11px]'` and keep var(--pm-text-dim). Consider using var(--pm-font-ui) under modern instead of mono for consistency with the rest of the modern chrome (it currently forces mono). No s | Perform any action (edit a command, delete a selection, Save) and confirm the message appears below the button bar and is legible in dark + light. |
| Overwrite confirmation (alertdialog) | `features/shortcuts/ShortcutEditor.tsx` | dialog | — | Keep role=alertdialog and the checkbox as `input[type=checkbox]` inside it (BOTH pinned by tests: querySelector('[role="alertdialog"] input[type="checkbox"]')). Swap the raw checkbox for the <Checkbox> primitive (data-slot=checkbox -> accent-color var(--pm-acc | Create New with a key that already exists (e.g. an existing binding) -> the alertdialog appears. Toggle 'do not show this again' (checkbox should show the indigo accent under modern), click Confirm (applies) or Cancel (dismisses). Verify in dark + light. |
| CreateShortcut form (key-capture + command) | `features/shortcuts/ShortcutEditor.tsx` | form | — | Wrap in <Panel> or gate `modern && 'rounded-[8px] border-[var(--sh-line-strong)]'` on .scmodal__create. Replace both field <input>s with <TextInput className="scmodal__field-input..."> KEEPING the readOnly, value, placeholder, data-testid, and the exact onKeyD | Click 'Create New' -> the form appears. Focus the Key field and press a chord (e.g. Ctrl+G) — it displays PyMOL's name; press a reserved key and confirm nothing appears. Type a Command; Create enables only when both are filled. Check both fields have modern ra |

## apps/web/src/features/keyboard

_A headless global keyboard bridge — a document-level keydown listener that forwards keystrokes to the PyMOL engine. It renders NO user-facing UI (no panels, dialogs, toolbars, forms, or controls) and has no CSS, so there is nothing to convert for the design-system / light-mode work._

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
| KeyboardService | `features/keyboard/KeyboardService.tsx` | service | — | N/A — no visual conversion possible or needed. There is no JSX, no className, no inline styles, no BEM classes, and no CSS file in features/keyboard (directory contains only KeyboardService.tsx, register.ts, and the excluded KeyboardService.dom.test.tsx). It r | Nothing to see. It is invisible by design. To confirm it is wired and doing its (non-visual) job: open the app front-end at http://100.71.244.15:3005, click into the 3D viewport, and press a bound key (e.g. arrow / PageUp / a labeled shortcut); observe the eng |

## apps/web/src/features/picking

_NO SUCH DIRECTORY: apps/web/src/features/picking does not exist. The only 'picking' directory in the repo is packages/viewport/src/picking/, which is a non-web package of pure pick-resolution logic (ray.ts, pick.ts, route.ts, index.ts + *.test.ts) — no .tsx, no .css, no React components, and no user-facing UI. There are zero components to convert for a design-system pass. (Picking-related UI does not live in its own feature; click-to-pick is wired through builder/viewportPicking.ts, mouse/useMouseMode.ts, and properties/ — all outside any 'picking' directory. If the orchestrator meant one of those features, re-target it.)_

| Component | File | Kind | prim | Modernize action | Verify |
|---|---|---|---|---|---|
